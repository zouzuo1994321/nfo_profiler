# 变更记录（Changelog）

本软件采用**双版本号**：

- **对外大版本**：语义化 `v主版本.次版本.修订号`（如 v1.1.0）；
- **内部版本号**：`YYMMDD + 当日迭代号(4 位)`（如 `2609060001` = 2026-09-06 第 1 次构建）。

每次迭代在此记录变更，并同步更新 [README.md](./README.md) 的「版本历史」章节。

---

## v1.6.0（内部版本 2609170008 · 2026-09-17）—— 向量编辑：双击「手动权重」直接编辑 + 按钮字号缩小 + 打包体积回归

### 需求

1. **双击「手动权重」单元格可直接编辑**该向量的手动权重；
2. 行内「屏蔽 / 删手动」按钮**字号再小一号**。

### 改动（gui.py）

- **双击编辑**：`cellDoubleClicked` 接 `_vec_cell_double_clicked`——仅响应「手动权重」列
  （col=4），行数据取维度条目 UserRole（与排序解耦）；`QInputDialog.getDouble`
  弹窗（范围 ±20、两位小数，回显当前值，无手动覆盖时回显 0）；
  保存走 `upsert_vector_override`：无覆盖 = 新增、有 = 覆盖、输入 0 = 屏蔽；
  取消不落库；刷新表格并状态栏提示；
- **按钮字号 12 → 11px**（`_VEC_BTN_QSS`）；
- 顶部提示文案补充「双击『手动权重』列都能改向量」。

### 附带修复：打包体积 112MB → 89.7MB（spec 去重 DirectML）

- **根因**：共享 venv（多项目共用）里同时存在 `onnxruntime`(CPU) 与
  `onnxruntime_directml` 两套发行版，后者把 DLL 装进了嵌套目录
  `onnxruntime/onnxruntime/capi/` → `DirectML.dll`(9.1MB)×2 + `onnxruntime.dll`(6.5MB)×2
  被 PyInstaller 全收且重复 → exe 从 81.6MB 涨到 **112.7MB**；
- **修复**（`nfo_profiler.spec`）：
  1. `excludes` 增加 `onnxruntime_directml`（另含 pandas / matplotlib / scipy /
     sklearn / torch / torchvision）；
  2. `a.binaries` 过滤掉全部 `directml.dll` 与 `onnxruntime/onnxruntime/` 嵌套重复树，
     只保留 `onnxruntime/capi/` 下的正式一份；
- **说明**：不要把 numpy / onnxruntime 整个 exclude —— 那是本次走错的弯路，会让
  MiniLM 本地模型推理静默失效（`ai_engine` 有异常隔离，不报错只降级）。
  最终 89.7MB 与历史 81.6MB 的 8MB 差额来自 venv 的 onnxruntime 被升到 1.29.0，
  属 venv 漂移、非代码问题。

### 验证

- `tools/smoke_v160.py`（offscreen，monkeypatch `QInputDialog.getDouble`）**10 项全过**：
  版本号；QSS 字号 11px；双击新增 3.5 / 覆盖 -1.0 / 输入 0 屏蔽 / 取消不落库 /
  其他列不触发；屏蔽回归；`grab()` 渲染。
- 打包体积 **112.7MB → 89.7MB**（DirectML 去重生效）；
  发布版 exe 离屏启动（`NFO_NO_SPLASH=1` + `QT_QPA_PLATFORM=offscreen`）
  14s 后进程仍存活，无启动崩溃。

### 版本

- 对外 `v1.6.0`（小功能 + 打包修复，次版本 +1），内部 `2609170008`。

---

## v1.5.0（内部版本 2609170006 · 2026-09-17）—— 启动闪屏：logo + 应用名 + 真实进度条 + 淡入淡出

### 需求

软件开启前有几秒等待，希望在打开前加入启动闪屏：**logo.png + 下方「NFO 画像矿工」
标题 + 淡入淡出特效 + 载入进度百分比进度条 + 底部一行小字版权**。

### 改动

- **新增 `nfo_profiler/splash.py` · `SplashScreen`**（QSplashScreen 子类，自绘）：
  * 深色圆角面板 + **logo 居中**（复用 `find_data_file("logo.png")`，源码/冻结双模式）+
    **「NFO 画像矿工」大标题** + **渐变进度条**（#a63a78→#ff5fb8，与应用主题一致）+
    左侧阶段提示 / 右侧百分比 + **底部版权小字**（COPYRIGHT_NOTICE）；
  * **淡入淡出**：`QPropertyAnimation` 动画 `windowOpacity`（淡入 620ms / 淡出 380ms），
    淡出结束 `finish(win)` 交棒主窗口；
  * `setProgress(pct, tip)` 更新后立即 `repaint() + processEvents()`——主线程构建 UI
    期间进度条也能刷新（否则会"冻结"）。
- **真实进度**（而非假动画）：`MainWindow` 新增 `progress_cb` 回调，在真实载入阶段
  上报——初始化 4% → 配置 10% → 数据库 18% → 图标 24% → **8 个功能模块各 32%~92%** →
  数据源 95% → 明细过滤 98% → 就绪 100%；`_init_ui` 的 8 个 Tab 按计划表逐个上报。
- `run_gui`：创建闪屏 → 淡入 → 构建 `MainWindow(progress_cb=…)` → 主窗口 `show()` 后
  置 100% 并淡出；环境变量 `NFO_NO_SPLASH=1` 可关闭（离屏测试 / 无 GUI 环境）。

### 验证

- `tools/smoke_v150.py`（offscreen）**18 项全过**：版本号；闪屏尺寸 / 进度更新与封顶 /
  越界夹取 / `grab()` 真实绘制 / 淡入淡出无异常 / 文案常量；**MainWindow 真实进度回调
  ≥8 阶段、百分比递增、最终 100、含 8 个模块提示**；`NFO_NO_SPLASH` 降级路径。
- 离屏渲染预览图人工核对排版（logo / 标题 / 进度条 / 版权位置正确；
  预览环境缺 CJK 字体显示方框，真实 Windows 用系统微软雅黑正常）。

### 版本

- 对外 `v1.5.0`（新功能，次版本 +1），内部 `2609170006`。

---

## v1.4.9（内部版本 2609170005 · 2026-09-17）—— 向量编辑：按钮回归（紧凑 UI）+ 排序后重建

### 现象 / 需求

v1.4.8 为根除「显示不全」改成纯右键菜单，交互过于隐蔽。用户要求：**保留按钮，
但对按钮 UI 进行优化**。

### 改动（gui.py）

- **按钮回归 + 紧凑化**：
  * 专用样式 `_VEC_BTN_QSS`（padding 1×6、min/max-height 20/22、字号 12），
    按钮定高 22；
  * **关键坑**：`QHBoxLayout` 默认把按钮**纵向拉伸到行高**（实测撑到 26px），
    须 `setAlignment(AlignLeft | AlignVCenter)` 才能真正保持 22px；
  * 列宽重排：维度 76→64、四个数字列 92→76（给操作列让位）、操作列 64→124。
- **排序后重建按钮**（`_vec_rebuild_buttons`）：保留作品明细式原生排序，
  但 cell widget 不跟随排序移动，故监听 `sortIndicatorChanged` + 渲染后调用，
  依据维度条目 **UserRole 数据**重建，保证「按钮 ↔ 行数据」永远对应；
- 行右键菜单保留（与行内按钮同一组动作）。

### 验证

- `tools/smoke_v149.py`（offscreen）**11 项全过**，含**显示完整性的直接判据**：
  按钮组宽度不超出操作列（124px）、按钮右边缘不超出表格视口；按钮高度 22；
  排序后「删手动」按钮与手动权重行一一对应；屏蔽 / 删手动回归；`grab()` 渲染。

### 版本

- 对外 `v1.4.9`（UI 返工，次版本 +1），内部 `2609170005`。

---

## v1.4.8（内部版本 2609170004 · 2026-09-17）—— 向量编辑：根除操作列显示不全 + 对齐作品明细的原生排序

### 现象 / 需求

1. v1.4.7 单按钮方案下「操作」列**显示仍不全**（截图实测：按钮贴右缘被裁）；
2. 表头排序要求**参考作品明细**的交互（原生表头 ▲/▼）。

### 根因与方案（gui.py）

- **根因**：QTableWidget 的 cell widget **不参与列宽/滚动/视口计算**，无论怎么设列宽，
  最后一列控件都可能被视口裁掉——只要操作列嵌控件，问题就无法根除；
- **方案**：操作列**彻底移除全部控件**（v1.4.5-7 的按钮/菜单按钮全部去掉，列仅 64px 占位），
  操作统一为**行右键菜单**（🚫 屏蔽 / 🗑 删手动覆盖，行数据存维度条目 UserRole，
  与排序状态解耦——排序后行号会变，不能按行号回查）；
- **排序对齐作品明细**：改用**原生** `setSortingEnabled(True)`（点表头出 ▲/▼、同列切换升降序）；
  数字列用新增 `_VecNumItem`（QTableWidgetItem 子类，`__lt__` 按存储浮点值比较，
  "—" 按 -1e18 排最后），修复"9.00 > 12.00"的文本序错误；填充期间关排序、
  填完恢复并按当前指示 `sortItems`（默认生效强度倒序）。

### 验证

- `tools/smoke_v148.py`（offscreen 全窗口）**11 项全过**：版本号；**全表无 cell widget**；
  原生排序开启；名称列升/降序互为倒序（Qt 本地化拼音排序，非 Python 码点序——
  断言写法已相应修正）；数值列按数字倒序（非文本序）；排序后右键数据不错位
  （UserRole 元组）；屏蔽 / 删手动回归；`grab()` 渲染。

### 版本

- 对外 `v1.4.8`（UI 返工，次版本 +1），内部 `2609170004`。

---

## v1.4.7（内部版本 2609170003 · 2026-09-17）—— 向量编辑：操作改下拉菜单 + 表头排序

### 现象 / 需求

1. v1.4.6 虽固定了「操作」列宽，行内双按钮仍显得拥挤（用户反馈未修复，要求改按钮 UI）；
2. 表头需要**点击排序**功能。

### 改动（gui.py）

- **操作列 UI 重做（v1.4.7）**：双按钮 → **单按钮「▾ 操作」+ QMenu 下拉**
  （「🚫 屏蔽（正负权重清零）」/「🗑 删手动覆盖」按行状态出现），另支持**行右键菜单**
  （同一组动作，`customContextMenuRequested`）；操作列宽 168 → 96，彻底摆脱列宽挤压；
- **表头点击排序**：`sectionClicked` → 数据侧排序（`_vec_apply_sort` + `_vec_render`，
  不走 QTableWidget 原生排序，避免 items 与 cell widget 错位）；同列再点切换升/降序，
  异列给默认方向（数字列倒序、文本列正序）；表头显示排序指示箭头；操作列不参与排序；
- `vector_refresh` 拆分：构建行数据（`_vec_rows`）→ 排序 → 渲染（`_vec_render`）。

### 验证

- `tools/smoke_v147.py`（offscreen 全窗口）**12 项全过**：版本号；操作列宽 96、单按钮 +
  QMenu；名称列升序 / 同列切换降序 / 数字列默认倒序；屏蔽 / 删手动回归；`grab()` 渲染。
  *（首轮「同列切换降序」断言写错——误要求升降序首行相同，属测试 bug，已修正。）*

### 版本

- 对外 `v1.4.7`（功能 + UI 返工，次版本 +1），内部 `2609170003`。

---

## v1.4.6（内部版本 2609170002 · 2026-09-17）—— 修复向量编辑「操作」列按钮被裁切

### 现象 / 需求

向量编辑表格的「操作」列未设列宽策略（默认 Interactive，其余列为默认宽度），
被 Stretch 的名称列挤压：行内「屏蔽 / 删手动」按钮被压扁、裁切，数字列宽随内容漂移。

### 改动（gui.py · `_build_vector_editor_tab` / `vector_refresh`）

- **列宽策略**：维度 76px、四个数字列各 92px、操作列 168px 固定；名称列独占 Stretch；
  `setStretchLastSection(False)`；
- **按钮定高 24**、行高 32（`verticalHeader().setDefaultSectionSize`）、关闭 WordWrap；
- 数字列（自动正权 / 自动负权 / 手动权重 / 生效）文本**居中**；
- 操作按钮 cell 边距调整为 4/2/4/2、间距 6。

### 验证

- `tools/smoke_v146.py`（offscreen 全窗口）**10 项全过**：版本号；名称列 Stretch、
  六列固定宽度断言、行高 32、按钮定高 24；屏蔽 / 删手动功能回归；`grab()` 渲染。

### 版本

- 对外 `v1.4.6`（UI Bug 修复，修订 +1），内部 `2609170002`。

---

## v1.4.5（内部版本 2609170001 · 2026-09-17）—— 智能推荐 · 向量编辑模块

### 需求

在 ④作品推荐 子页签（随机推荐 / 智能推荐 旁）新增**🧬 向量编辑**模块：可从
**艺人 / 导演 / 标签 / 片商** 等维度查看智能推荐已自动生成的偏好向量（投票画像），
并可手动添加 / 覆盖 / 屏蔽向量，实现**手动快速迭代推荐算法**。

### 改动

- `store.py`：
  * 新增 `vector_overrides` 表（`kind`/`name` 主键，`weight`/`note`/`updated_at`），
    旧库打开自动建表（schema 全 `CREATE TABLE IF NOT EXISTS`）；
  * 新增 `vector_overrides()` / `upsert_vector_override()` / `delete_vector_override()`，
    非法 kind、空名称直接拒绝。
- `recommender.py`：
  * **导演维度入画像**：`_movie_tokens` 纳入 `director:`（与 tag/actor 同权重 2.0），
    `_df_lookup` 支持 director 的 IDF 查询；
  * **手动向量合并**（`_apply_vector_overrides`）：在 IDF **之后**叠加——
    `weight>0` 加正权、`weight<0` 加负权（软排斥）、`weight=0` **屏蔽**（自动正负清零，
    即"删除自动向量"）；显式用户意图不做 IDF 衰减；
  * 新增 `vector_snapshot()`：返回 `auto_pos/auto_neg`（纯投票画像）+ `pos/neg`
    （生效权重）+ `manual`（手动覆盖清单），供 GUI 展示三方对照。
- `gui.py`：
  * ④作品推荐 第 3 个子模块「🧬 向量编辑」：手动向量添加表单（维度 / 名称 / 权重）+
    维度过滤 + 名称过滤 + 「只看手动向量」+ 刷新；
  * 向量总表 7 列：维度 / 名称 / 自动正权 / 自动负权 / 手动权重 / 生效(正/负) / 操作，
    按生效强度倒序；行内操作按钮「屏蔽」「删手动」；
  * 首次切到该 Tab 自动加载；每次改动状态栏提示「回智能推荐点再推荐一批立即生效」。

### 验证

- `tools/smoke_v145.py`（offscreen 全窗口）**25 项全过**：版本号；store CRUD（覆盖 /
  删除 / 非法 kind / 空名称拒绝）；导演维度自动入画像；手动正权 / 负权 / 屏蔽三类合并
  语义；snapshot auto/eff 分离；GUI 三个子模块齐全、手动向量出表、屏蔽后手动权重 +0.00、
  删手动恢复、名称过滤 / 只看手动、`grab()` 真实渲染。
- 回归：`smoke_v144.py` 功能项全过（仅版本断言因 bump 失败，符合预期）。

### 版本

- 对外 `v1.4.5`（新功能，次版本 +1），内部 `2609170001`。

---

## v1.4.4（内部版本 2609160003 · 2026-09-16）—— 修复「点赞演员共享」恒定命中同一位演员

### 现象 / 需求

智能推荐右下角兜底位（构成要求「与 👍 作品共享演员 ≥1 部」）批批都是同一位点赞演员
的作品（实测恒为 JULIA）。要求：如果该位置由「点赞演员共享」触发，应避免老是推荐
同一位被点赞过的演员。

### 根因（recommender.py · `_liked_actor_ids`，v1.4.0 引入）

三个确定性因素叠加，导致每批命中结果恒定：

1. 演员集合按 `sorted(actors)` **字母序**遍历，永远从排序最靠前的演员开始找；
2. 内层 `SELECT DISTINCT movie_id ... ORDER BY 默认序` **无随机**，首个未排除 id 恒定；
3. **没有演员级「最近已用」记忆**——即便作品换了一部，演员还是同一位。

### 改动

- `nfo_profiler/recommender.py`：
  * `_liked_actor_ids` 重写为**两步查询**（保留原语义：与点赞作品共享演员的
    **全库**作品，而非仅点赞作品本身）+ **演员级轮换**：
    - 一次查出点赞演员 → 按 actor 聚合其全库作品（`actor -> [movie_id...]`）；
    - **演员级避让**：近 `RECENT_FORGET`(12) 批内已作为该要求来源的演员
      （`_req_actors: actor -> 批次号`）不再选中；
    - 从剩余候选中**随机选演员**、再从其作品中**随机取** cap 部；
    - 极端情况（所有演员都在避让期内）清空避让防死锁，构成要求仍可兜底；
    - 选中后登记演员批次号，与 `_seen` 同周期瘦身。
  * `Recommender.__init__` 新增 `self._req_actors`；`reset_recent()` 同步清空；
  * `smart_picks` docstring 补充演员级轮换说明。

### 验证

- `tools/smoke_v144.py` **13 项全过**：版本号；前置（5 位点赞演员各 8 部作品）；
  **连续 5 批命中 5 位不同演员**（避让的确定性推论）；`_req_actors` 登记一致；
  避让期全满时清空防死锁仍可兜底；无点赞作品返回空；smart_picks 全流程两批
  18 部 / 带演员 / 两批有轮换 / 无重复 id。
  *（调试中修正两处测试问题：movie_id 从 1 起；每部作品双演员会让「按首位演员断言」失真，
  改为单演员数据集。实现侧同步发现并修复了一处回归：候选作品须查演员**全库**作品，
  不能只限点赞作品本身。）*
- 回归：`smoke_v141.py` / `smoke_v143.py` 功能项全过（仅版本号断言因 bump 失败，符合预期）。

### 版本

- 对外 `v1.4.4`（Bug 修复，修订 +1），内部 `2609160003`。

---

## v1.4.3（内部版本 2609160002 · 2026-09-16）—— 流光效果返工：环绕流动（不再闪烁）

### 现象 / 需求

v1.4.2 的「流光」实为整圈边框颜色往返渐变，观感像**闪烁**；要求参照环形流光效果
（高光段沿边框转圈的 conic 渐变），颜色保持粉色、**速度放慢**。

### 改动

- `nfo_profiler/gui.py` · `MovieCard` 流光重写：
  * 边框底色固定为**中粉** `#a63a78`（QSS `selected` 态 2px），外发光恒定
    （`#ff5fb8`、blurRadius=22，**不再脉动**——消除闪烁感）；
  * 新增 `paintEvent` 重绘：`QConicalGradient`（角度随时间推进）描边圆角矩形，
    **亮粉高光段**（#ffd9ef → #ff7fc4）沿边框渐隐回底色、尾部渐入衔接成环，
    高光段即绕边框**环绕流动**，与参考站点效果一致；
  * 速度放慢：40ms/帧 × 1.5°/帧 ≈ **9.6s 每圈**（v1.4.2 约 1.2s/来回）；
  * 每帧仅 `update()` 重绘选中卡片本身，开销可忽略。

### 验证

- `tools/smoke_v143.py`（offscreen 真实实例化）**9 项全过**：版本号；选中 → 中粉底色边框 +
  恒定外发光 + 定时器启动；`_tick_glow` 角度随帧推进（流动而非整圈变色）；推进后 blurRadius
  仍恒定；`card.grab()` 真实触发 paintEvent 的 conic 描边渲染无异常；取消选中熄灭恢复。
- `py_compile` gui.py OK。

### 版本

- 对外 `v1.4.3`（界面效果返工，修订 +1），内部 `2609160002`。

---

## v1.4.2（内部版本 2609160001 · 2026-09-16）—— 粉色流光选中高光 + 去除导航白线

### 现象 / 需求

1. ④作品推荐「随机推荐 / 智能推荐」卡片选中高光升级为**粉色流光**；
2. 主导航 Tab 栏下方出现一条**白色横线**，要求去除。

### 改动

- `nfo_profiler/gui.py` · `MovieCard`：选中高光由静态「2px 蓝边」升级为**粉色流光**——
  新增 `_GLOW_COLORS` 粉色渐变环（#ff5fb8 → #ffa1d6 → #ff2f92 → #ffc4e4）+
  `QGraphicsDropShadowEffect` 同色外发光；`set_selected(True)` 启动 66ms/帧 QTimer，
  边框颜色随相位在渐变环上**往返流动**（约 1.2s 一个来回）、发光亮度同步脉动
  （blurRadius 18→28）；`set_selected(False)` 停表熄灭、恢复基础样式。
  仅选中的那 1 部卡片运行动画，其余卡片零开销。
- `nfo_profiler/gui.py` · 全局 QSS 新增
  `QTabBar { background:#1b1e23; qproperty-drawBase:0; }`——
  白线根因是 QTabBar 的**原生基线（drawBase）**未被 QSS 覆盖、按系统浅色调绘制；
  关闭后主导航 / 推荐子页签 / AI 分析子页签的基线一并消失。

### 验证

- `tools/smoke_v142.py`（QT_QPA_PLATFORM=offscreen，真实实例化 MovieCard）**8 项全过**：
  版本号；QSS 含 `qproperty-drawBase:0`；选中即进流光（3px 粉边 + 外发光开启）；
  连续 `_tick_glow` 40 帧出现 ≥3 种流光色（确在流动）；取消选中定时器停 + blurRadius=0 + 恢复基础样式。
- `py_compile` gui.py OK。

### 版本

- 对外 `v1.4.2`（界面小功能），内部 `2609160001`。

---

## v1.4.1（内部版本 2609150003 · 2026-09-15）—— 卡片 / 记录副信息：片商 → 演员名字

### 现象 / 需求

1. ④作品推荐「随机推荐」和「智能推荐」的卡片下方：**片商 → 演员名字**；
2. 「浏览记录」和「投票记录」表格：**片商列 → 演员列**。

### 改动

- `nfo_profiler/recommender.py` · 新增 `_attach_actors`：给推荐结果批量补 `actors`
  （分块 IN 查 `movie_actors`，只填缺失键、已带演员不重复查；一次 ≤18 部开销可忽略）。
  `random_picks`（随机推荐卡片）/ `smart_picks`（智能推荐 + 兜底补齐条目）/
  `similar_picks`（基于选中作品推荐卡片）三条出口全部接上。
- `nfo_profiler/gui.py` · `MovieCard`：副标题改读 `movie["actors"]`
  （无演员时回退片商），按宽度截断并加 ToolTip 悬停看完整名单；
  截断上限由「半行」放宽为整行（演员名比片商更值得展示）。
- `nfo_profiler/gui.py` · 投票记录 / 浏览记录表格：表头「片商」→「演员」，
  数据列改读聚合演员名（超 20/18 字省略号截断）。
- `nfo_profiler/store.py` · `vote_records` / `play_records`：SQL 增加
  `(SELECT GROUP_CONCAT(a.actor, '、') FROM movie_actors a WHERE a.movie_id=…) AS actors`
  关联子查询（单行一次索引查找，700 条记录实测无感知）。

### 验证

- `tools/smoke_v141.py`（离屏冒烟）**11 项全过**：版本号；random_picks / smart_picks /
  similar_picks 均带 `actors` 且来自 `movie_actors`（不再混入片商名）；
  `vote_records` / `play_records` 返回 `actors` 聚合字段且值正确。
- `py_compile` gui / recommender / store 全 OK。

### 版本

- 对外 `v1.4.1`（界面小功能，次版本内修订），内部 `2609150003`。

---

## v1.4.0（内部版本 2609150002 · 2026-09-15）—— 智能推荐重构：关键词偏向 + 构成要求软补充 + 扫描/写库提速 + 性能基准

### 现象 / 需求

1. ④作品推荐 第二个子页签「相似作品（智能推荐）」直接改名为「**智能推荐**」；
2. 在智能推荐中加入**关键词输入**，可偏向「演员 / 标签 / 视频名称 / 视频标题」等；
3. 继续优化智能推荐（共 18 部）的**构成**：尽可能保证 2 部为半年内、2 部为 1 个月内、
   1 部演员与「点赞过」作品共享；
4. **优化数据源与扫描效率**；
5. 改动后做**性能基准测试**（进程数 / 每批文件数 / 数据库总数 的推荐值）。

### 改动

- `nfo_profiler/gui.py` · `_build_recommend_tab`：第二子页签标题由
  「🎯 相似作品（智能推荐）」改为「🎯 智能推荐」；全模块状态文案同步去「相似作品」前缀。
- `nfo_profiler/gui.py` · 智能推荐模块工具条：新增**关键词输入框** `edt_rec_smart_kw`
  （占位提示「演员 / 标签 / 番号 / 标题…」，`setClearButtonEnabled` + `returnPressed`
  即触发刷新；点清除按钮文本变空时自动刷新去掉偏向）。
- `nfo_profiler/recommender.py` · `random_picks` / `_random_picks_locked`：
  新增 `keyword` 参数。命中「演员 / 标签 / 番号 / 标题」任一字段的作品会被**强制拉进候选池**
  并整体上浮（`jitter×2`，随偏好尺度自适应，不被淹没），让本批推荐明显偏向该关键词；
  LIKE 对 `%`/`_` 转义，避免通配符语义异常。新增辅助 `_keyword_ids`。
- `nfo_profiler/recommender.py` · `smart_picks` 改为「个性化基础流 + 构成要求软补充」：
  先取个性化基础流（18 部，含关键词偏向、最近已推避让，轮换由它驱动），
  再在基础流之上**软性补充**构成要求——尽量覆盖 近半年（180 天）≥2 部、
  近 1 月（30 天）≥2 部、与任意「👍」作品共享演员 ≥1 部；这些仅作兜底补齐、
  **不强制置顶、不抢占前排**，且同样避让「最近已推」，保证「再推荐一批」正常换内容、正常轮换；
  其余由基础流补齐，不足用随机兜底；全程硬排除 👎 + 去重。新增 `_recent_ids` / `_liked_actor_ids`。
- `nfo_profiler/store.py` · 连接初始化新增**性能 PRAGMA**：`cache_size=-64MB`、
  `mmap_size=256MB`、`temp_store=MEMORY`、`journal_size_limit=64MB`（WAL 已启用）。
- `nfo_profiler/store.py` · `upsert_records`：子表（标签 / 演员 / 导演 / 技术标签）
  写入由「每部一次 `executemany`」改为**整批累积后一次性 `executemany`**，
  大库扫描的子表写往返从约 4×N 次降到 4 次。
- `nfo_profiler/scanner.py`：小库多进程回退阈值 `<50` → `<200`
  （基准实测：微型 NFO 多进程 spawn/IPC 开销反而拖慢，详见基准报告）。

### 验证

- `tools/_test_v140_rec.py`（临时逻辑测试）**全过**：智能推荐 18 部构成要求
  （近半年≥2 / 近1月≥2 / 点赞演员≥1）、关键词偏向（标签 / 演员）均生效。
- `tools/_test_v140_store.py`（临时测试）**全过**：`upsert_records` 新增 / 更新 / 批量
  三种情形子表均不丢不重（更新单部标签后正确替换为新值）。
- `tools/bench_v140.py` → `output/benchmark_v140.md`：推荐随库规模 2k→30k 仅 51ms→113ms；
  写库 6000 条吞吐 ~1.9万~2.6万 条/s（每批 1000–2000 达平台）；
  **扫描进程数反直觉结论**：微型 NFO 1 进程最快（2412 条/s）、8 进程最慢（117 条/s）。
- `py_compile` 全模块 OK。

### 版本

- 对外 `v1.4.0`（智能推荐能力重构 + 扫描/写库提速 + 基准，次版本 +1），内部 `2609150002`（2609150002 重新打包：智能推荐构成要求软性化 + 基准报告补推荐配置表）。

---

## v1.3.9（内部版本 2609140002 · 2026-09-14）—— ④推荐交互统一「单击高亮/双击播放」+ 智能推荐扩到 18 部 + 打开/响应提速

### 现象 / 需求

1. 「基于选中作品推荐」面板（随机模块下方）借鉴「相似作品（智能推荐）」的交互：
   **单击仅高亮、双击播放**；
2. 「相似作品（智能推荐）」从 2 行 12 部增加到 **3 行 18 部**；
3. 优化软件打开速度，尤其 ④作品推荐 响应慢、容易「未响应」。

### 改动

- `nfo_profiler/gui.py` · `MovieCard`：缩略图改为**异步加载 + 缓存**
  （新增模块级 `_ImageTask`（`QRunnable` + `QThreadPool`），读盘 + 高质量缩放放进线程池，
  主线程只负责上屏；原图 / 已缩放图缓存到 `_base_img` / `_base_pm`，
  `resize_image` 直接复用、**不再每次缩放都重新读盘 + 9 次 `os.path.isfile` 查找**）；
  图片路径只解析一次（`_img_path`）。这是「切到 ④作品推荐 未响应」的根因——
  原 `_load_image` 在每次 relayout 同步读 20~40 张（常位于较慢磁盘 / 网络卷）缩略图。
- `nfo_profiler/gui.py` · `_on_tab_changed`：推荐计算**延迟到首次进入 ④作品推荐 才触发**
  （`recommend_refresh` + `rec_smart_refresh` 后台线程计算），启动阶段不再提前算、
  也不在隐藏 Tab（宽 0）上布局；之后进入只按实际空间
  `_relayout_recommend(refresh=False)`（卡片已满不重算）。
- `nfo_profiler/gui.py` · `_fill_card_row`：基于选中面板卡片 `on_select` 由 `None` 改为
  `_on_similar_card_selected`（单击仅高亮，与智能推荐一致）；新增该方法（在 `rec_similar_cards` 内高亮）。
- `nfo_profiler/gui.py` · 常量 `REC_SMART_N=18` / `REC_SMART_COLS=6` / `REC_SMART_ROWS=3`
  （仍 6 列 × 3 行、可上下滚动）。
- `nfo_profiler/__init__.py`：`__version__="1.3.9"` / `__build__="2609140002"`。

### 验证

- `tools/smoke_v139.py`（新增，离屏冒烟）**10 项全过**：版本 bump / 随机 6 /
  单击驱动下方（12）/ 双击随机播放 / 智能 18 / 单击智能仅高亮 / 再推荐一批（18）/
  双击智能播放 / 基于选中面板单击仅高亮且列表不变 / 双击基于选中面板播放；全程无 abort。
- `py_compile` gui.py / __init__.py / smoke_v139.py 均 OK。

### 版本

- 对外 `v1.3.9`（交互统一 + 容量扩充 + 性能，修订 +1），内部 `2609140002`。

---

## v1.3.8（内部版本 2609140001 · 2026-09-14）—— ④作品推荐拆「🎲 随机 / 🎯 相似作品（智能推荐）」双模块 + 多元推荐体系

### 现象

用户希望 ④作品推荐 的「相似作品（智能推荐）」更聪明、体系更多元，并明确交互语义：

1. **引入更多元的推荐体系**：在原有 `random_picks` / `similar_picks` 之外，新增
   `smart_picks`（个性化 / 探索位更多 / 多样性更高），与随机推荐形成互补双体系；
2. **④作品推荐拆成两个独立模块**（QTabWidget）：
   - **🎲 随机推荐**：单击卡片 → **选中该作品并刷新下方「基于选中作品推荐」面板**
     （与选中的那部最像的 12 部），**双击 → 播放**；
   - **🎯 相似作品（智能推荐）**：完全独立的个性化推荐流，
     **单击 → 仅高亮、绝不更新推荐列表**，**双击 → 播放**，单独增加
     **「🔄 再推荐一批」** 按钮换一批；
3. 内部版本号按用户要求迭代到 **2609140001**（当日日期 + 当日第 1 次构建）。

### 根因 / 设计

旧版 ④作品推荐 把「随机」和「相似」混在一个滚动流里，单击语义单一、没有独立的
「智能推荐」入口，也没有「再推荐一批」的显式换批能力。本次：

- 把 ④作品推荐 重构成 `QTabWidget` 两个子模块，各自独立加载、刷新、布局；
- 单击语义按模块区分：
  - 随机模块卡片 `on_select = _on_recommend_card_selected`（驱动下方相似面板）；
  - 智能推荐模块卡片 `on_select = _on_smart_card_selected`（**仅高亮，不重算列表**）；
  - 下方「基于选中作品推荐」面板卡片 `on_select = None`（单击无效、双击播放）；
- 新增 `Recommender.smart_picks`，在 `random_picks` 基础上把
  `explore=3`（探索位，vs 随机 `explore=1`）、`diversity=0.5`（多样性，vs 随机 `0.35`）
  调高，并默认开启近半年加权 / 多次浏览未投降权，整体更「多元」；
- `_chunks`（SQLite 参数分块工具）保持**模块级**函数：被模块函数 `similar_by_id`
  直接调用，不能误缩进成类方法（v1.3.8 初版曾因把 `def _chunks` 头吞掉、又误缩进成
  4 空格类方法，导致 `SyntaxError: 'yield' outside function` 与
  `NameError: name '_chunks' is not defined`，最终修正为 0 缩进模块级）。

### 修复 / 改动

- `nfo_profiler/__init__.py`：版本号 `__version__ = "1.3.8"`、`__build__ = "2609140001"`；
- `nfo_profiler/recommender.py`：新增 `smart_picks(limit=12, explore=3, diversity=0.5)`
  （委托 `random_picks`，开启 `recent_boost_on` / `browse_demote_on`）；
- `nfo_profiler/gui.py`：
  - 常量新增 `REC_SMART_N=12` / `REC_SMART_COLS=6` / `REC_SMART_ROWS=2`；
  - `_build_recommend_tab` 重写为 `QTabWidget` 双模块
    （模块1：顶部 bar + 上下 `QSplitter`：上随机 6 部居中、下「基于选中作品推荐」12 部可滚；
     模块2：bar 含 `btn_rec_smart`「再推荐一批」+ `lbl_rec_smart_stat` + 独立 12 部可滚流）；
  - `_fill_card_row` 的 `which` 支持 `smart`，按模块设 `on_select`；
  - `_relayout_recommend` 增加 smart 重排（`rec_smart_cards`）；
  - 状态新增 `rec_smart_cards: List[MovieCard] = []`；
  - 新增 `rec_smart_refresh` / `_on_rec_smart_done` / `_on_smart_card_selected`（仅高亮）
    / `_on_rec_tab_changed`（切模块时按实际空间重排，规避隐藏 Tab 尺寸 0）；
  - 投票联动两处把 `rec_smart_cards` 纳入同步；`_on_votes_changed` 末尾加
    `QTimer.singleShot(350, self.rec_smart_refresh)`；
  - 进入 Tab 时延迟触发：`QTimer.singleShot(200, recommend_refresh)` /
    `250, rec_smart_refresh` / `400, _relayout_recommend(refresh=True)`。

### 验证

- `tools/smoke_v138.py`（新增，离屏冒烟）**8 项全部通过**：
  1. 版本号 bump 到 v1.3.8 / 2609140001；
  2. 随机推荐加载 6 部；
  3. 单击随机卡片 → 下方「基于选中作品推荐」刷新（12 部，单击驱动下方）；
  4. 双击随机卡片 → 触发播放；
  5. 切到相似作品（智能推荐）模块 → 独立加载 12 部；
  6. 单击智能卡片 → 仅高亮、推荐列表不变；
  7. 点「再推荐一批」→ 刷新后仍 12 部、按钮重新可用、无崩溃；
  8. 双击智能卡片 → 触发播放；全程无 abort / 无残留线程。
- 引擎直连验证：`random_picks(6)` / `smart_picks(12)` / `similar_picks(12)` 均正常返回。

### 版本

- 对外 `v1.3.8`（推荐体系扩充 + 交互语义拆分，修订号 +1），内部 `2609140001`。

---

## v1.3.7（内部版本 2609120005 · 2026-09-12）—— 随机推荐质量再优化（近半年加权 / 多次浏览未投降权 / 点踩硬排除）

### 现象

用户希望「随机推荐」更聪明：

1. 加强**近半年（180 天内）**新作品的权重；
2. 降低「**多次浏览但一直没点赞或点踩**」的作品出现概率；
3. **点过 👎 的作品绝不再出现**在随机推荐中。

### 根因

`Recommender.random_picks` 原先只按偏好分 + 分层采样 + MMR 重排出结果，没利用三类现成信号：

- 作品上映时间 `premiered` / 入库时间 `dateadded`（近半年信号完全没参与排序）；
- `play_history.play_count`（多次浏览却没投票 → 用户其实没那么感兴趣，但原算法无降权）；
- 已投 👎 集合只在 `exclude_voted` 里和 👍 一起被过滤，没有「硬排除」的语义区分。

### 修复

- 新增常量：`RECENT_DAYS=180`、`RECENT_BOOST_FRAC=0.7`、`BROWSE_PENALTY_THRESHOLD=3`、`BROWSE_PENALTY_STEP=0.12`、`BROWSE_PENALTY_CAP=1.2`；
- **① 近半年加权** `_recency_boost`：解析 `premiered`/`dateadded`（优先 premiered），落在 180 天内按
  `frac = 1 - days/180` 加权 `jitter * 0.7 * (0.45 + 0.55*frac)`——**随档内 jitter 自适应缩放**，
  避免被偏好分（实测 raw 高达 86~385）淹没；整段窗口都受益而非只抬最近一月；
- **② 多次浏览未投降权** `_browse_penalty`：批量拉 `play_history.play_count`，对已投票作品不降权，
  未投票且 `play_count > 3` 时每多 1 次扣 `0.12*jitter`（封顶 `1.2*jitter`）；
- **③ 点踩硬排除**：`down_ids = {mid for mid, v in voted.items() if v < 0}`，在 `exclude_voted`
  过滤后再做一层 `rows = [r for r in rows if r["id"] not in down_ids]`，👎 永不进候选池；
- SQL 多拉 `premiered, dateadded, year` 字段；`random_picks` 新增 `recent_boost_on` / `browse_demote_on`
  开关（默认开，GUI 调用方式不变）。

### 验证

- `tools/verify_rec_v137.py`（新增）全部通过（生产行为，含 v1.3.4 避让）：
  - ① 近半年占比 **3.8% → 19.0%**（↑15.2，权重确实生效）；
  - ② 多次浏览未投票出现 **13 → 5 部**（↓）；
  - ③ 👎 出现 **0**（硬排除生效）；
  - 无投票历史路径近半年占比 7.25% → 36%；健壮 5 项全 OK；
- 回归 `tools/verify_rec_v134.py` 全部通过：v1.3.4 的 A/B/C/D（IDF / MMR / 避让）未被破坏，纯加法改动；
- `tools/_sweep_recency.py` 隔离 v1.3.4 避让后定量校准：`RECENT_BOOST_FRAC` 取
  `0 / 0.3 / 0.5 / 0.8 / 1.0` → 近半年占比 `5.3% / 7.3% / 8.0% / 30.7% / 44.0%`，选 **0.7**
  （生产行为下 3.8% → 19.0%）。

### 工程小记

- **`sqlite3.Row` 没有 `.get()` 方法**！`hasattr(row, "get")` 为 False，`row.get(key)` 会
  **静默返回 None** → 近半年加权永远 0、改前改后无差异。必须用
  `try: v = row[key] except (KeyError, IndexError, TypeError): v = None`。
  `tools/_debug_recency.py` 的 unit test 实测 `parsed = None` 才定位到根因。
- **加权幅度必须随 `jitter` 自适应缩放**：偏好分 raw 实测 86~385，固定小常量（如 12）会被淹没；
  且线性衰减只抬最近一月效果差，改用 `jitter*FRAC*(0.45+0.55*frac)` 让整段窗口都受益。

### 版本

- 对外 `v1.3.7`（推荐质量优化，修订号 +1），内部 `2609120005`。

---

## v1.3.6（内部版本 2609120004 · 2026-09-12）—— 修复「浏览记录里点 👍 就闪退」

### 现象

④作品推荐 →「🕘 浏览记录」里点 👍，软件瞬间消失，没有任何提示。

### 根因：QThread 在还在运行时被销毁（Qt qFatal → 进程 abort）

复现脚本（`tools/repro_browse_vote.py`）在离屏下跑出的退出码是
**`-1073740791`（0xC0000409 = abort）**，并伴随
`QThread: Destroyed while thread '' is still running`。

链路是：

1. 点 👍 → `_cast_vote` → `on_changed()` → `_on_votes_changed` →
   `QTimer.singleShot(300, recommend_refresh)` → 启动 `_Worker` 后台算推荐；
2. `_Worker.run()` 在 **run() 内部**发出 `done` 信号（emit 完才 return）；
3. 信号经队列回到主线程时，工作线程**可能还没从 run() 返回**；
4. 旧的 `_cleanup()` 直接 `worker.deleteLater()` → 延迟删除在主线程执行时
   线程仍在跑 → Qt 析构 QThread 触发
   `qFatal("QThread: Destroyed while thread is still running")` → **abort**。

这是一个**竞态**，所以表现是"有时点一下就没了"，难以稳定复现。
（顺带：关窗口时也会踩同一条路 —— `closeEvent` 只弹确认框，没真正停线程。）

### 修复

- **`_cleanup()` 先等线程结束再释放**：`isRunning()` → `quit()` + `wait(3000)`
  → `deleteLater()`。因为信号是从 `run()` 末尾发出的，`wait()` 通常几毫秒内返回；
- **新增 `_shutdown_workers()`**：统一停止并回收所有后台线程，
  `closeEvent` 与 `run_gui()` 退出前都会调用（防止关窗口 / 解释器收尾时 abort）；
- **`BrowseHistoryDialog._cast_vote` 延后重建表格**：原先在按钮自己的
  `clicked` 槽里调 `reload()`，会连带销毁**正在发信号的按钮**（sender），
  属于典型 use-after-free；改成 `QTimer.singleShot(0, self.reload)`；
- **崩溃兜底 `_install_crash_guard()`**：未捕获异常不再无声闪退 ——
  写入 `output/crash.log` + 弹窗提示（PySide6 6.11 对槽函数异常会直接 abort）。

### 验证

- `tools/smoke_v136.py`（新增）7 项全过：连续 5 次推荐刷新、浏览记录连续
  点赞 3 次（表格重建后仍可点击）、`_shutdown_workers` 清空线程表、
  崩溃兜底已安装、v1.3.5 图标/悬停无回归；**退出码 0**（修复前是 abort）；
- `tools/repro_browse_vote.py`：修复后退出码 0，不再出现 QThread 警告；
- 回归 `smoke_v135`（6 项）+ `smoke_v12`（7 项）+ `smoke_v133`（6 项）全过。

### 工程小记

- **「点一下就闪退、无提示」= Qt qFatal/abort**，不是普通 Python 异常；
  离屏跑一遍看**退出码**比看日志更快定位（`-1073740791` / `0xC0000409`）。
- **QThread 的 `done`/`failed` 若在 `run()` 内部 emit，回收必须先 `wait()`**，
  否则必踩 "Destroyed while thread is still running"。
- **不要在控件的信号槽里销毁该控件**（尤其 `setCellWidget` 重建表格），
  用 `QTimer.singleShot(0, ...)` 推到下一个事件循环。
- 离屏冒烟里 **`app.quit()` 会连带触发主窗口 `closeEvent`**（进而关库），
  需要多段事件循环时用 `time.sleep + processEvents()` 的事件泵代替。

### 版本

- 对外 `v1.3.6`（修 Bug，修订号 +1），内部 `2609120004`。

---

## v1.3.5（内部版本 2609120002 · 2026-09-12）—— 推荐卡片悬停大图 + 全新图标

### 变更

- **④作品推荐：悬停卡片即浮动显示该作品大图**（与③作品明细同款交互）：
  - `MovieCard.enterEvent` → 复用 MainWindow 的 `_show_image_popup`，
    在鼠标旁浮出大图（fanart → thumb → poster 同一优先级，最长边 360px）；
  - `leaveEvent` → 收起弹窗（与投票按钮的显隐同一时机，互不干扰）；
  - 无图作品悬停不弹窗、不报错；图片路径命中卡片已有的 `_img_path` 缓存，
    不产生额外磁盘查询。
- **全新图标 / logo**：根目录 `logo.png` 与 `logo.ico` 全部换成新的粉色 NFO 放大镜图标：
  - `logo.png` = logo2.png 原图（1024×1024）；
  - `logo.ico` = 由 logo2.png 重新生成的**7 尺寸** ICO
    （16/24/32/48/64/128/256），exe 图标、窗口图标、任务栏图标三处一致；
  - 旧图标备份在 `history/logo_old_v134.png` / `.ico`；
  - Web 界面（`/api/logo`、favicon）复用 logo.png，自动跟随更新。

### 验证

- `tools/smoke_v135.py`（新增）6 项全过：悬停弹窗显示且像素非空（366×249）、
  离开收起、无图占位不弹窗、logo.png 与 logo2.png 逐字节一致、
  logo.ico 含 7 尺寸、窗口图标非空，v1.3.2/v1.3.3 入口无回归；
- `tools/make_logo_v135.py`（新增）：备份旧 logo → 复制新图 → Pillow 生成多尺寸 ico；
- 回归 `tools/smoke_v12.py`（推荐 Tab 7 项）全过。

### 工程小记

- **PySide6 6.11+ 的 `enterEvent` 形参是 `QEnterEvent`**（不再是 `QEvent`），
  离屏冒烟里手动触发悬停要构造 `QEnterEvent(QPointF, QPointF, QPointF)`，
  传 `QEvent` 会 TypeError。
- **venv 里跑脚本装包后别用 `-S`**：`-S` 会跳过 site-packages，
  刚装好的 Pillow 立刻 `import PIL` 失败 —— `make_logo_v135.py` 第一跑就栽在这里。
- 换图标后**资源管理器可能仍显示旧图标**（Windows 图标缓存），
  不是打包问题；改名文件或清图标缓存即可看到新图标。

### 修复（2609120003）：任务栏/标题栏图标一直是系统默认图标

**用户反馈打包后任务栏和标题栏左上角仍是白底默认图标。** 用 ctypes 探针
（`tools/probe_icon.py`，`WM_GETICON` 三档）拿到决定性证据：打包 exe 的
SMALL2 / BIG / SMALL **三档全 NULL** → 程序从未成功 `setWindowIcon`，
任务栏只能回落 Qt 默认窗口类图标（IDI_APPLICATION）。

根因（两层叠加，且一直静默）：

1. **PyInstaller 6.x 把 `--add-data "src;dest"` 的 `dest` 当目录处理**：
   打的是 `logo.ico;logo.ico`，归档里实际条目是 `logo.ico\logo.ico`
   （`config/synonyms.json` 同样翻倍）。运行时 `os.path.exists(_MEIPASS/logo.ico)`
   永远为 False，`_set_icon` 的 `if os.path.exists(p)` 静默跳过；
2. **`QIcon(路径)` 解码失败也不报错**：就算文件在，插件缺失时返回空图标照样 set。

修复：

- **打包侧**：dest 改成目录语义 —— `--add-data "logo.png;assets"`、
  `--add-data "logo.ico;assets"`、`--add-data "config/synonyms.json;config"`，
  归档条目确认为 `assets\logo.ico` / `config\synonyms.json`；
- **代码侧**（不依赖打包姿势）：
  - `gui.find_data_file(name)` 统一按候选链定位
    （资源目录 → `assets/` → PyInstaller 6 嵌套形态 `name/name` → exe 旁）；
  - `_set_icon` 逐个候选验证 `QIcon.availableSizes()` 非空才采用，
    并同时设**应用级图标**（QMessageBox 等全部继承）；
  - `normalize.resolve_config_paths` / `webui._serve_logo` 补同样的候选链。
- **实测**：重打包后探针三档 `WM_GETICON` 全部返回真实句柄
  （`0xED20E67` / `0x60E0FBB`），任务栏/标题栏/Alt-Tab 生效。

### 版本

- 对外 `v1.3.5`（小功能，次版本 +1），内部 `2609120003`。

---

## v1.3.4（内部版本 2609120001 · 2026-09-12）—— 「换一批」真的换一批：推荐去重四步改造

### 背景：客观重复率只有 5.8%，为什么还是觉得"翻来覆去都一样"

用 `tools/diag_recommend_dup.py` 在真实库（48,622 部 / 已投票 85 部）上连刷 20 次实测：

| 指标 | 实测 | 对照 |
|---|---|---|
| 客观重复率 | **5.8%**（120 槽位 / 113 唯一作品） | 很低 |
| 相邻两批重叠 | 0.05 / 6 部 | 几乎不重 |
| **跨批次 tag Jaccard** | **0.489** | — |
| 同批次 tag Jaccard | 0.493 | — |
| **全库随机两部（基线）** | **0.074** | — |

**跨批次 ÷ 基线 = 6.6 倍，跨批次 ÷ 同批次 = 0.99**。也就是说：点「换一批」
前后看到的作品，和同一批里的作品一样像 —— 算法并没有反复推同一部，
而是**推出来的全都长一个样**。

### 根因：泛化标签权重失控

`推荐得分 = Σ(正权重 − 负权重)`，而权重**只按投票次数累加，完全不考虑标签稀有度**：

| token | 权重 | 覆盖 | 占全库 |
|---|---|---|---|
| `tag:单体作品` | **170**（最高） | 27,504 | **56.6%** |
| `tag:中出` | 160 | 22,073 | 45.4% |
| `tag:DMM独家` | 150 | 13,925 | 28.6% |
| `tag:巨乳` | 62 | 18,569 | 38.2% |

结果：抽样 5,000 部里**只有 16 部（0.3%）得分是 0** —— 几乎全库都"命中偏好"，
得分挤在均值 309 附近，可选池高达 **18,602 部（38.3%）**，6 张卡全在这批
高度同质的作品里轮转。加上 85 票**全是 👍、0 个 👎**，负权重表为空，
没有任何"排除"信号。

### 变更（A / B / C / D 四项）

- **A · 泛化标签 IDF 降权**（治本，`_apply_idf`）：
  按全库稀有度加权 `log(N/df) / log(N/df_ref)`，clamp 到 `[0.05, 1.0]` ——
  **只衰减、不放大**（避免 df=1 的极稀有标签过拟合霸榜）。标题词没有 df 可查
  （要 LIKE 全表扫描），统一 `TITLE_DISCOUNT=0.5` 打折承认其噪声更大。
  改后 Top1 从「单体作品（覆盖 56.6%）」变成「人妻（覆盖 10.8%）」，
  精准标签（人妻 / 熟女 / 多P / 出轨）终于排到泛化标签前面。
- **B · 最近已推避让**（`_recent_penalty` / `_remember` / `reset_recent`）：
  记录每部作品上次出现在第几批，距上次 `gap` 批 → 惩罚 `-jitter × max(0.15, 1/gap)`；
  超过 12 批自动遗忘（同时控制内存）。直接消灭"刚看过又来"。
- **C · 批内 MMR 多样性重排**（`_mmr_select`）：
  贪心 `(1−λ)·相关性 − λ·与已选最大相似度`（λ 默认 0.35），每档只取前
  `MMR_POOL=150` 部精算，强制同批 6 部在 tag 上拉开差异。
- **D · 扩大候选池 + 分层采样**：
  `pool` 默认 500 → **2000**（候选从占全库 2.06% 提到约 8%）；
  按得分分位分**高 / 中 / 低**三档配额取（默认 3 / 2 / 1，低档为探索位），
  探索位只从**非负面**作品里取，避免推到用户明确讨厌的类型。

### 验证（`tools/verify_rec_v134.py`，同脚本改前/改后对照）

关闭 A/B/C/D 跑一遍当基线，再恢复跑一遍：

| 指标 | 改前 | 改后 | 变化 |
|---|---|---|---|
| 跨批次 Jaccard | 0.366 | **0.221** | ↓ 39% |
| 同批次 Jaccard | 0.348 | **0.199** | ↓ 43% |
| 重复率 | 12.5% | **4.2%** | ↓ 67% |
| 单部最高出现次数 | 3 | **2** | ↓ 33% |
| 重复过的作品数 | 13 | **5** | ↓ 62% |
| 单批耗时 | 0.14 s | 0.31 s | ↑（后台线程，可接受） |

健壮性 5 项全过：`limit=0`、候选仅 2 部（返回全部而非报错）、`pool=50`、
关掉 C/D、无投票历史的纯随机路径。
回归 `tools/smoke_v133.py`（6 项）+ `tools/smoke_v12.py`（7 项，推荐 Tab
6 张卡 / 投票 toggle / 相似推荐 12 张卡）全过。

### 工程小记

- **破坏性排序改造要先做"关得掉"的设计**：A/B/C/D 都能通过模块常量
  （`IDF_FLOOR` / `TITLE_DISCOUNT`）+ 参数（`explore` / `diversity` / `pool`）
  + `reset_recent()` 单独关掉，于是"改前 vs 改后"能在**同一个脚本、同一份数据**
  上对照，不用靠历史数字回忆。
- **候选池变大后 `IN (...)` 必须分块**：4000 个 id 直接拼 `IN` 会踩 SQLite
  参数上限，用既有的 `_chunks(seq, 400)` 分块。
- **候选不足时返回"少于请求数"是正确的**，不要为了凑数返回重复项
  （最初把 `pool=1` 期望写成返回 6 部，是测试断言错了，不是代码错了）。
- **IDF 的 clamp 上限设 1.0（只衰减不放大）**：设 >1 会让 df=1 的极稀有标签
  权重暴涨，推荐立刻过拟合到一两部作品上。

### 版本

- 对外 `v1.3.4`（推荐质量优化，修订号 +1），内部 `2609120001`。

---

## v1.3.3（内部版本 2609070008 · 2026-09-11）—— 独立「🧹 清理失效记录」入口 + 扫描列表自动带出数据源

### 背景：为什么勾了「清理」数字却不变

清理逻辑原本挂在**扫描收尾**（v1.3.0 设计），也就是说它只有在**完整跑完一次全量重扫**
之后才会执行。对 49,463 部 / 127 TB 的库来说，一次全量重扫要重新解析每一部作品
（几十分钟到几小时）；而：

- 仅仅勾选开关、没有真正跑完一次扫描 → **完全不会清理**；
- 扫描中途点了「终止」→ 磁盘遍历不完整，**刻意跳过清理**（否则会误删）；
- 新打开软件时扫描列表是空的，直接点「开始扫描」会先撞上「没有目录」警告。

所以「勾了但数字不变」并不是清理失效，而是**这条路径的门槛太高**。

### 变更

- **新增「🧹 清理失效记录」按钮**（扫描 Tab，与开始扫描/暂停/终止同一行）：
  - **只遍历目录、不解析 NFO** —— 拿磁盘上的 NFO 集合与库内做差集后删除，
    代价从"重新解析 5 万部作品"降到"列一遍目录"（快一个数量级）；
  - 后台线程执行，带进度显示（每个数据源分别报告发现多少 NFO / 清理多少条），可终止；
  - 完成后弹出结果、写日志、刷新数据源统计与「数据库概况」；
  - 执行前有确认框，说明影响范围与不可恢复性。
- **防误删护栏（重要）**：
  - `store.prune_missing` 在 `existing` 为空时**直接返回 0** ——
    网络盘掉线 / 盘符未挂载 / 路径写错时遍历结果是空集，照常做差集会把
    整个数据源几万条记录删光；现在宁可不清理；
  - 数据源目录不可访问 → 跳过并记警告，同样不清空。
- **启动时自动把已登记数据源填进扫描列表**（原来要手动点「＋ 已登记数据源」，
  否则点扫描会报「没有目录」）；
- **扫描终止时的提示**：勾了清理但扫描被终止 → 日志明确说明"本次未执行清理
  （避免误删），可点『🧹 清理失效记录』单独执行"；正常跑完但差集为 0 →
  说明"库与磁盘一致"。

### 实现

- `scanner.prune_missing_paths(store, roots, progress, should_stop)`：
  遍历 + 差集 + 逐源统计，返回 `(删除条数, 警告列表)`；
- `gui.PruneWorker(QThread)`：`progressed` / `succeeded` / `failed` 三个信号，
  复用扫描 Tab 的进度条与日志区；
- `gui.start_prune / _on_prune_done / _on_prune_error`。

### 验证

- `tools/verify_prune.py`（新增，临时目录 + 临时库，不碰真实数据）4 项全过：
  只删幽灵（2 真实 + 1 幽灵 → 剩 2）、子表与投票一并清理、**空集合零删除**、
  **不可访问目录跳过并告警**；
- `tools/smoke_v133.py`（新增）6 项全过：按钮与回调、启动时 7 个数据源自动就位、
  PruneWorker 可构造可终止、掉盘保护、v1.3.2 功能无回归；
- `tools/diag_missing.py`（新增诊断工具）：按目录缓存 listdir 批量比对，
  给出"库里有、磁盘上没有"的准确条数与分源明细，用于判断到底该清理多少条。

### 版本

- 对外 `v1.3.3`（功能新增，修订号 +1），内部 `2609070008`。

---

## v1.3.2（内部版本 2609070007 · 2026-09-07）—— 删除重复的「查看记录」入口 + 新增「浏览记录」

### 变更 · ④ 作品推荐

- **删除「查看记录」入口**：统计标签「共 n 部 | 👍x · 👎y」上的可点击
  「📑查看记录」与旁边的「📑 投票记录」按钮功能重复，本版移除——
  统计标签恢复为**纯文本展示**（去掉手型光标 / Tooltip / `eventFilter` 点击分支）；
- **新增「🕘 浏览记录」按钮**（工具条，换一批旁）：记录**双击播放过的作品**；
- **浏览记录对话框**（`BrowseHistoryDialog`）：
  - 表格列出 浏览时间 / 番号 / 标题 / 片商 / 次数 / 当前投票；
  - **每行可直接 👍 / 👎 补投**（再点同一按钮 = 撤销）——
    解决「打开看了但没投票」的作品无法回溯补票的问题；
  - 顶部统计「共 n 条 | 其中已投 👍x · 👎y | 未投 k 条」；
  - 删除选中（Del 键）/ 清空：**只删浏览痕迹，不动投票与作品库**；
  - 双击行 → **重新播放**该作品（同时自然刷新浏览时间）；
  - 📂 打开文件夹。

### 实现

- `store.py` 新增 `play_history` 表（`movie_id` 主键 → 重复双击只更新时间、`play_count +1`，
  `played_at` 索引）与方法 `record_play_by_path` / `play_records` / `remove_play` /
  `clear_plays` / `get_play_count`（全部持锁）；
- `play_records` JOIN `movies`（标题/片商）+ LEFT JOIN `preferences`（当前投票）；
- 挂钩点在 `_play_movie_video`：**推荐卡片双击**与**作品明细双击**两条播放路径都会落库；
- 数据源移除 / 失效清理（`remove_source` / `prune_missing`）同步清掉对应 `play_history`；
- `on_changed` 回调复用 `_on_votes_changed`：对话框里补票后，主窗口统计、
  卡片投票状态与推荐结果立即同步。

### 验证

- 新增 `tools/smoke_v132.py`（6 项）：查看记录入口删除（文案/光标/eventFilter 拦截）、
  浏览记录按钮、store 落库与重复播放计数、双击播放挂钩（monkeypatch openUrl）、
  对话框行内投票 toggle / 删除仅删痕迹，全部通过；
- 冒烟加固：离屏模式下把 `QMessageBox.*` 替换为「打印 + 默认返回」，
  防止 worker 异常弹窗导致永久挂死，并把真实异常暴露到日志；
- 回归 `tools/smoke_v13.py` 通过。

### 版本

- 对外 `v1.3.2`（功能新增，次版本 +1），内部 `2609070007`。

---

## v1.3.1（内部版本 2609070006 · 2026-09-07）—— ④作品推荐布局回调（随机 6 / 相关 12 两行可滚）

### 变更 · ④ 作品推荐

- **取消左右滑动**：v1.3.0 改成网格布局后 ◀/▶ 按钮已失效（没有横向滚动条可推），
  本版直接移除两个按钮，横向滚动策略设为 `ScrollBarAlwaysOff`；
- **随机推荐固定 6 部**：不再随窗口大小变化（`REC_RANDOM_N = 6`，6 列 × 1 行），
  卡片在区域内**居中**展示，尺寸仍随可用空间拉伸（1920×1080 下约 305×258）；
- **相关推荐固定 12 部**：`REC_SIMILAR_N = 12`，**6 列 × 2 行**，
  纵向滚动策略 `ScrollBarAsNeeded` —— **空间不足时上下滚动**查看；
- 上下区高度比改为 4:6（相关推荐区更大，两行卡片更容易完整展示）；
- 数据源「移除」按钮补 `minimumWidth(56)`，避免被压成「移…」。

### 实现要点

- 新增常量 `REC_RANDOM_N/COLS/ROWS`、`REC_SIMILAR_N/COLS/ROWS`；
- `_rec_metrics(area, cols, rows)`：卡片宽 = (可用宽 − 间距)/列数（140–420），
  行高 = (可用高 − 间距)/行数（120–280），图片高 = 行高 − 信息区 − 6；
- `_build_card_row(vertical_scroll, center)` 参数化两个区；
- `_fill_card_row` 按 tag（random/similar）取对应尺寸与容量并裁剪。

### 验证

- `tools/smoke_v13.py`：随机 6 部 + 无 ◀/▶ 残留 + 横向滚动关闭、
  相关 12 部（6×2）+ 纵向滚动 AsNeeded，全部通过；
- 回归 `smoke_gui` / `smoke_v115` / `smoke_v12` 全部通过。

### 版本

- 对外 `v1.3.1`（同日布局回调，修订号 +1），内部 `2609070006`。

---

## v1.3.0（内部版本 2609070005 · 2026-09-07）—— 六项体验迭代（推荐 UI / 投票管理 / 扫描清理 / 画像与聚类 UI）

### ① ④ 作品推荐：UI 顶满页面（卡片网格自适应）

- 卡片流从「单行横向滚动」改为**网格铺满**：先按 215px 参考格估算能放几列 / 几行，
  再把卡片**拉伸到刚好填满**可用区域（卡片宽 150–400、图高 100–320 夹紧）；
- `MovieCard.apply_size(w, h)` 支持运行时改尺寸并重排内部控件（标题截断长度随宽度自适应，
  图片按新尺寸重新缩放，避免放大后模糊）；
- 推荐数量随容量走：1920×1080 下 9 列 × 2 行 = **18 张**铺满整页（v1.2.0 固定 10 张细条）；
- 窗口缩放 / 切换 Tab 时自动重排（节流 250ms；非当前页收不到 resize，
  因此额外绑定 `currentChanged` 触发重排）；
- 卡片数超过容量自动裁剪，不留纵向滚动条。

### ② 投票记录管理器（👍 / 👎 可查看、可删除）

- 顶部「? 投票记录」按钮 + 统计标签可点击（手型光标 + Tooltip 提示）→ 弹出管理器；
- 支持 **全部 / 👍 / 👎** 三种筛选，表格列出 投票 / 番号 / 标题 / 片商 / 时间；
- **删除选中**（支持 Del 键）、**清空当前筛选**、**双击打开所在文件夹**；
- 删除后自动刷新偏好统计并重算推荐（投过的口味变化立即体现在随机刷新里）；
- 新增 `store.vote_records()`（JOIN movies 取番号标题）、`vote_summary()`、`clear_votes()`。

### ③ 已登记数据源：移除按钮显示不完整

- 根因：「操作」列没有固定宽度，被前面几列挤到几乎不可见；
- 修复：列宽策略改为 目录列 Stretch + 数字列 ResizeToContents + **操作列固定 78px**，
  按钮改为「容器 + 布局」承载（红色危险样式、带数据源路径 Tooltip），行高 30。

### ④ 关闭增量扫描 → 全量重扫并清理已删除文件

- 扫描选项新增「**全量重扫时清理已删除文件的记录**」：
  勾选增量扫描时自动禁用；**取消增量扫描即自动勾选并解锁**；
- 实现：扫描收尾把「库里有记录、但本次磁盘遍历没找到 NFO」的条目删掉
  （`store.prune_missing(root, existing)`，含 movies / files / 各子表 / 投票）；
- **零额外磁盘 IO**：复用本次收集到的文件路径集合做差集，不重复 stat；
- 结果栏与日志新增「清理失效 N 条」计数。
- 单元验证：造 2 条真实 + 1 条幽灵记录 → 只删 1 条，未误伤存在文件。

### ⑤ ② 画像概览：生成后 UI 迭代

- 新增「**一句话画像**」摘要条：数据源 / 作品数 / 容量 / 年份跨度 / 均分
  + 偏好标签 top5 + 标题高频 top3 + 常看艺人 top3；
- KPI 卡片区改为**自适应列数**（3–8 列，随宽度重排）并放进滚动区，每张卡加图标；
- 维度表改为 **# / 名称 / 数量 / 占比条** 四列，底部显示「共 N 项，合计 X」；
- 新增「📋 复制维度」「💾 导出当前维度 CSV」。

### ⑥ ⑦ AI 分析：聚类细化 + 分析入口 UI 迭代

聚类算法 `cluster_themes` 细化（v1.3.0）：

- **组合主题名**：不再只叫单个种子词，而是 `种子 × 最强关联 × 次强关联`
  （如「人妻 × 熟女 × 中出」），并自动去重；
- **泛化词过滤**：覆盖 >55% 作品的超级通用词（「单体作品」等）、显式停用词、
  纯数字年份，都不再当种子也不进成员表；
- **二级子主题**：覆盖 >20% 的大簇在簇内作品集上再做一次局部共现 + 贪心，
  产出最多 4 个子主题（各带规模、占比、2 部代表作）；
- **簇统计**：作品数 + 占全库比例 + 平均评分 + 年份跨度；
- **过小簇过滤** `min_size`；成员带**共现强度 / 共现作品数**明细。

分析入口 UI：

- 「🎯 分析入口」重做：番号输入 + 三个动作 + **聚类参数区**
  （候选词 / 最小共现 / 最小作品数 / 含标题词）+ 提示；
- 聚类结果树渲染升级：主题 → 📊统计 → 🔸二级子主题（含代表作）
  → 关联标签（带共现强度）→ 关联标题词 → 代表作品（top5，双击打开）；
- 新增「💾 导出聚类结果」（TXT 报告 / CSV 表格）。

### 验证

- 新增 `tools/smoke_v13.py`：7 项全通过（窗口 / 卡片自适应网格 / 投票记录删除 /
  数据源操作列 / 扫描联动 / 画像新 UI / 聚类细化）；
- 回归：`smoke_gui` / `v113` / `v114` / `v115` / `v12` 全部通过（49,436 部副本库）；
- 聚类实测：10.6s 产出 11–14 个主题 + 3 个二级子主题（v1.2.0 为 15 个无细分主题）；
- `prune_missing` 单元验证通过。

### 工程小记

- **离屏冒烟的 `wait_workers` 陷阱**：worker 列表一开始为空会立刻返回，
  而首刷是 `QTimer.singleShot(200/400)` 触发的 → 必须设最小泵事件时长（≥3s）；
- **离屏下非当前 Tab 不 resize**：子控件尺寸停留在构造默认值（640×480），
  断言卡片尺寸前要先 `tabs.setCurrentIndex()`；
- **离屏下 `cellWidget` 可能未完成布局**：按钮 `width()` 会返回异常值，
  断言改用 `sizeHint()`；
- **模态框禁令**：离屏下 `QMessageBox` 永久阻塞，冒烟不要调用会弹确认框的方法，
  改为直接走「确认后」的同一段逻辑。

### 版本

- 对外 `v1.3.0`（跨 4 个 Tab 的体验迭代 + 新增删除/清理能力），内部 `2609070005`。

---

## v1.2.0（内部版本 2609070004 · 2026-09-07）—— 作品推荐模块 / 悬停预览 / 双击播放 / 相似推荐提速

### 新增 · ④ 作品推荐模块（新主 Tab，位于 ③ 作品明细 与 ⑤ 重复检测 之间）

面向数万部作品的「个性化刷片」场景，完整闭环 = 随机刷新 → 看片 → 投票 → 越推越准：

- **🎲 随机推荐区**：
  - 「🔄 换一批」刷新 10 部作品，缩略图卡片流展示，◀/▶ 按钮左右滑动；
  - 每张卡片右上角 **👍 / 👎** 按钮（悬停卡片时浮现），点击记录偏好；
  - **偏好学习算法**（`nfo_profiler/recommender.py`）：
    - 投票历史聚合为正 / 负 **token 权重表**（tag / actor 权重 2.0，studio / 标题分词 1.0）；
    - 随机刷新先 `ORDER BY RANDOM()` 抽 500 部候选池，再按
      「正命中 − 负命中 + 探索抖动」排序取 top 10 ——
      投票越多越贴合口味，抖动（`max(2.0, 最强得分×0.6)`）保证不收敛到同几部；
    - 已投过票的作品默认从随机池排除（全投过则不排除）；
  - 投票后 300 ms 自动刷新一批，直观体现「不断优化」。
- **相似推荐区（下栏）**：点选任意卡片，下方用同一套缩略图卡片流展示
  10 部相似作品（tag + actor Jaccard，复用推荐引擎的 `similar_by_id`）；
- **卡片交互**：单击选中（高亮 + 触发相似推荐）；**双击播放视频**
  （同名前缀 `ABC-123.mp4` / `ABC-123-cd1.mkv` 优先 → 目录内任意视频，
  打不开则回退打开文件夹）；👍/👎 按钮三态样式（✅ 已赞 / ❌ 已踩 / 默认），
  换一批后投票状态从库中恢复。
- **数据落地**：新表 `preferences(movie_id, num, vote, voted_at)`，投票 toggle 语义
  （重复点同一按钮 = 撤销），索引 `idx_preferences_vote`。

### 变更 · ③ 作品明细

- **缩略图改为鼠标悬停显示**：开启 `mouseTracking` + `itemEntered` 信号，
  悬停即弹出图片预览（不再需要双击）；鼠标离开表格自动隐藏；
- **双击行为改为播放视频**：优先播同目录同名视频，找不到 / 打不开则打开文件夹；
- 「高频标题词」维度的 `(v1.1.4 新增)` 版本标注文字已清理。

### 性能 · 相似推荐重写（关键修复）

- **根因**：旧 `similar_picks` / `ai_engine.similar_works` 对每部候选作品
  发 3 次独立 SQL；热门标签（如「单体作品」共现 27,927 部）下候选集数万部，
  **10 万+ 次小查询，单次耗时 > 5 分钟**，推荐 Tab 表现为「永久卡死」。
- **重写**（`recommender.similar_by_id`，推荐模块与 AI 分析共用）：
  1. 两条 `GROUP BY` 聚合查询直接得到每个候选的共享 tag / actor 数；
  2. 按共享总数预排序，仅保留前 200 部进入精算（候选池截断）；
  3. 对前 200 部用分块 `IN` 查询批量拉全量数据，计算精确 Jaccard 取 top N；
  - 查询次数 O(标签块数 + 常数)，与库规模无关；
- **新索引**：`idx_tags_tag ON movie_tags(tag)`、`idx_actors_actor ON movie_actors(actor)`，
  schema 自动创建（首启对 31 万行 tag 表建索引约 1 s，一次性）；
- **实测**（49,436 部 / 316,847 条 tag）：最坏情况 **> 5 分钟 → 0.73 s（冷）/ 0.07 s（热）**。

### 稳定性 · 线程安全

- 推荐模块两个 worker（随机刷新 / 相似计算）可能**并发共用同一 sqlite3 连接**，
  触发 `Recursive use of cursors not allowed`；
- 修复：`Recommender.vote / profile_tokens / random_picks / similar_picks`
  全部整段持 `store.lock()`（RLock 可重入）串行化；
  投票 toggle（查→改两步）也加锁保证原子性。

### 验证（`tools/smoke_v12.py --db <副本库>`，49,436 部作品）

- 8 Tab 顺序正确（④ 作品推荐位于明细与重复检测之间）；
- 随机推荐 10 张卡片、投票 toggle 落库与撤销、相似推荐 10 张卡片全通过；
- 明细悬停预览 / 双击播放方法挂载 OK（`▶ 播放：DDK-173.mp4`）；
- 维度文字清理 OK；
- 回归：`smoke_gui` / `smoke_v113` / `smoke_v114` / `smoke_v115` 全部通过（副本库）。

### 工程小记

- 用户主程序正开着时测试：用 `sqlite3` backup API 导副本库，
  **不要直接 cp 主库 + WAL**（得到空库）；主库被占用时写入会报
  `attempt to write a readonly database`；
- Git Bash 里 Windows Python 收到 `/tmp/x.db` 会解析成**当前盘符**下的
  `\tmp\x.db`，跨工具传路径务必用完整 Windows 路径；
- 离屏冒烟中 `QMessageBox.critical` 会**永久阻塞**（模态框无人关闭）——
  worker 异常在离屏测试里表现为「挂死」而非报错，优先排查并发 DB 访问。

### 版本

- 对外 `v1.2.0`（新模块属次版本级变更），内部 `2609070004`。

---

## v1.1.5（内部版本 2609070003 · 2026-09-07）—— 修复筛选报错 / 作品明细表头排序 / 双击图片预览

### 修复

- **修复「应用筛选」报错**（用户反馈）：在「标签 / 演员」输入关键词后点击「应用筛选」，
  弹出 `OperationalError: no such column: m.id`。
  - **根因**：`search_movies()` 的 `tag_actor` 条件用了 `m.id`（别名 `m`），
    但 COUNT 与 SELECT 的 `FROM` 子句写成了 `FROM movies`，没给表加 `m` 别名，
    SQLite 解析时找不到 `m.id`。
  - **修复**：`FROM movies` → `FROM movies m`，并把 SELECT 列也改成 `m.*` 前缀，
    保证别名一致。

### 新增 · ③ 作品明细表头排序

- 列头点击即可排序：支持 **番号 / 标题 / 年份 / 发行 / 片商 / 导演 / 评分 / 时长 / 分辨率**；
- 再次点击同一列切换升序 / 降序，表头显示 Qt 原生排序箭头；
- 不支持排序的列（id / 路径 / 标签 / 演员）点击无影响；
- 使用 SQL 白名单映射 `_DETAIL_SORT_MAP`，避免字符串拼接导致的 SQL 注入；
- 排序字段后追加 `, m.id` 保证分页稳定。

### 新增 · ③ 作品明细双击图片预览

- 双击行仍打开文件夹，**同时**在鼠标右下方浮动显示同目录下的影片图：
  - 优先 `-fanart.jpg`；
  - 没有则 `-thumb.jpg`；
  - 没有则 `-poster.jpg`。
- 图片按 360 px 最大值等比缩放，3 秒后自动隐藏；
- 如果目录下没有任何匹配图片，方法安全返回，不影响打开文件夹；
- 弹窗用 `Qt.WindowType.ToolTip` + `QPixmap`，零外部依赖。

### 验证（`tools/smoke_v115.py`，49,436 部作品）

- 标签/演员关键词 `中出` 筛选：200 行 / 共 24,594 条，无 SQL 报错；
- 表头排序：标题列升→降切换正常，不支持列被忽略；
- 双击行：打开文件夹 + 图片预览方法健壮性 OK（无图时不崩溃）；
- 回归：`smoke_gui.py` / `smoke_v113.py` / `smoke_v114.py` 全部通过。

### 工程小记

- PySide6 的 `QTreeWidgetItem.child` 问题在 v1.1.4 已踩过；本次注意保持 `child(k).text(col)`。
- `QLabel(None, Qt.WindowType.ToolTip)` 做浮动预览比 `QToolTip` 更稳，后者对富图/大图支持差。
- `pm.size() + QSize(6, 6)` 给边框留一点 padding，避免贴边。

### 版本

- 对外 `v1.1.5`，内部 `2609070003`（同日第 3 次构建，修 Bug 修订号 +1）。

---

## v1.1.4（内部版本 2609070002 · 2026-09-07）—— 高频标题词 + 综合主题聚类 + AI 引擎开关

### 新增

- **② 画像概览 · 新维度「高频标题词」**：很多作品的 `<tag>` 打得不全，
  但 **title 里其实泄露了题材特征**（「极品黑丝」「OL 健身房」「美腿人妻」等）。
  新增 `nfo_profiler/title_tokenizer.py` —— 纯 Python 中文优先分词（已知词典 + N-gram + 停用词），
  不引入 jieba/scikit 等外部依赖。先前向最大匹配地把已知偏好词（tag + 内置常识词）切出整词，
  再对剩余中文段做 2-4 字 N-gram 滑窗；同一 title 内重复 token 仅计一次，避免「美腿美腿美腿」加权过高。
- **② 画像概览 KPI 卡新增「标题词数」**：展示库内累计识别出的不重复标题短语数。

### 改动 · ⑥ AI 分析 · 综合主题聚类升级

- **算法升级** `cluster_tags` → `cluster_themes`：
  - **不再只看点**：把 `tag` 与 `title_tokenizer` 输出的 `title_term` 当作**同质 token** 做共现分析；
  - 贪心聚类用「Jaccard 强度」取代单纯共现数，并加 `MAX_MEMBERS_PER_CLUSTER=12` 上限，
    避免「单体作品」这种覆盖 80% 作品的种子把所有 token 全吸走（之前只剩 1-2 个庞大簇）；
  - 高频种子用**自适应 min_co**，频率越高要求越严；
  - 每个主题额外返回 **代表作品 top3**（含 `path`、`num`、`title`、`rating`），可双击打开文件夹。
- **UI 重塑**：QTreeWidget 改成「🏷️ 主题 / 💬 主题(标题词种子)」三层结构
  - **关联标签**（蓝色）— 抽出来的关联 tag；
  - **关联标题词**（紫色）— 抽出来的关联标题短语；
  - **代表作品**（绿色）— top3，含真实 `path`，双击行直接打开对应文件夹。
- 全列宽靠 `setSectionResizeMode(Stretch)` 自适应。

### 改动 · ⑥ AI 分析 · 引擎开关

- **顶部状态条新增三颗按钮**：
  - **🚀 开启 AI** ：调用 `ai_engine.set_enabled(True)`，后台线程启动探测链路
    （Ollama → MiniLM → 内置启发式，三段优先级降级）；
  - **⏹ 关闭** ：关闭 AI 增强，回到「零依赖离线」模式；
  - **🔄 自检** ：关闭后立即重开，强制重新探测（适合刚装好 Ollama / MiniLM 之后的场景）。
- **状态条文案随状态自适应**：
  - 未开启：「🔕 AI 增强未开启 ｜ 点击 🚀 开启 AI 启用（内置本地启发式兜底）」；
  - 探测中：「⏳ AI 后端正在探测（后台线程）…」；
  - 就绪：「🤖 AI 增强已开启 ｜ 后端：heuristic ｜ 能力：keyword_expand, synonym_search ｜ …」。
- **轮询刷新**：`_refresh_ai_state` 在「探测中」状态每 800 ms 自身调度一次，把最新状态推到状态条。

### 数据流

- `ai_engine.similar_works`（v1.1.3 已加 `path`）继续保留，无改动。
- `cluster_tags` 保留为 `cluster_themes(..., with_title_terms=False)` 的兼容封装；
  已有的调用方（HTML 报告 / CLI 报告）继续工作。

### 验证（`tools/smoke_v114.py`，49,436 部作品下）

- [1] 窗口构建 OK · v1.1.4 (Build 2609070002)
- [2] KPI 卡新增「标题词数」：**48,454**
- [3] 高频标题词维度行数 40，Top 5：**`2024(10113), 中出(6832), 2025(6717), 2023(5541), 2022(4611)`**
- [4] AI 引擎按钮 OK：默认可开启 / 关闭灰
- [5] 开启 / 关闭 / 自检 切换全部正常，state 正确转为 ready
- [6] 综合主题聚类：15 个主题，覆盖 **86 个标签 + 57 个标题词**
- [7] 新三层结构 OK：关联标签 / 关联标题词 / 代表作品（含 path）
- [8] 双击代表作品节点正确触发 `_on_cluster_tree_double_clicked`
- [9-10] 相似推荐 / 画像解读 回归正常
- 旧 `smoke_gui.py` / `smoke_v113.py` 全部通过 → 老流程未破坏

### 工程小记

- **build_report_data 注意**：`top_tags` 这种 `*_n` 参数名要对应，
  一开始误用了不存在的 `top_n` 变量，build 期间报 `NameError`，
  改成 `top_title_terms(top_n=top_tags)` 即可。
- **PySide6 的 QTreeWidgetItem.child**：在新版只接受 1 个参数（行号），
  列要用 `child(k).text(col)`，**不要写 `child(k, col)`**。
- **耗时口径**：`top_title_terms` 在 49k 库上跑 **2.5 秒**，
  `cluster_themes` 跑 **8.8 秒**；前者放进 `build_report_data` 后
  整次画像概览从 ~10 s 涨到 ~22 s，仍在可接受范围。

### 版本

- 对外 `v1.1.4`，内部 `2609070002`（同日第 2 次构建，UI 主功能调整）。

---

## v1.1.3（内部版本 2609070001 · 2026-09-07）—— 明细双击打开 / AI 分析三栏重构

### 改动 · ③ 作品明细

- **双击打开文件位置**：检索结果双击任一单元格 → 用资源管理器打开对应 NFO 所在的文件夹；
  路径直接走首列 `UserRole` 数据，避免重复 SQL 查询。
- **UI 重构**：
  - 顶部紧凑工具栏（搜索 / 每页 / 分页 / 折叠筛选按钮），不再占两行；
  - **筛选可折叠**：右上角的「▼ 筛选」按钮点击切换展开/收起，给结果区让位；
  - 主表格新增「路径」列（默认隐藏，靠「显示『路径』列」开关按需开启）；
  - 主表格 13 列（id / path / num / title / year / premiered / studio / director /
    userrating / runtime_min / resolution / tags / actors），标题列 `Stretch`；
  - 表格下方新增行级操作区：**📂 打开选中文件夹** / **📋 复制路径** / **显示『路径』列**，
    行被选中时按钮自动启用；支持多选批量操作。
- **明细检索 SQL**：增加 `path` 字段一行，避免点击时再二次查询。

### 改动 · ⑥ AI 分析

- **三栏 TabWidget**：从单一 `QTextBrowser` 拆为 3 个子页：
  - **🔍 相似作品推荐**：从 `QTextBrowser` 改成 `QTableWidget`，列含 # / 番号 / 标题 / 片商
    / 评分 / 相似度 / 共享标签 / 共享演员 / 路径；双击行 → 打开对应 NFO 文件夹（同样靠
    `UserRole` 数据）；支持多选「打开选中文件夹」/「复制番号」。
  - **🧩 标签主题聚类**：从纯文本换成 `QTreeWidget`，主题为父节点、成员标签为子节点；
    支持全展开/全折叠/复制为文本。
  - **📊 画像解读**：保留 `QTextBrowser` 但升级为富 HTML（带后端徽标 + 时间戳 +
    版权声明），并支持「📋 复制全文」/「💾 导出到文件…」。
- **AI 引擎状态条**：标签页顶部新增状态行，显示后端类型、能力列表、初始化状态。
- **任务切换友好**：触发相似推荐 / 标签聚类 / 解读后，自动 `ai_tabs.setCurrentIndex`
  切到对应结果页。

### 数据流改动

- `ai_engine.similar_works()` 返回的字典新增 `path` 字段（与 `movies.path` 对齐），
  调用方不用再二次查库即可定位文件。

### 工程经验

- PySide6 测试 `SignalInstance.connect()` 等接口：直接 `emit(dummy_item)` 验证健壮性
  比 `receivers(<信号字符串>)` 更稳（后者在新版 PySide6 签名严格化）。
- 离屏模式下，未 `show()` 的子控件 `isVisible()` 一直是 `False`，但 `isHidden()` 才是
  真正的状态指标；冒烟脚本里二者不能混用。

### 验证

- 新增 `tools/smoke_v113.py`：
  - 13 列主表格 + 默认隐藏 id/path ✓；
  - 双击信号挂载 ✓；
  - 49,436 部作品下明细检索 → 22,999 条命中，首行 path `Y:\【02】Jav严选\DDK-173【鷹宮ゆい】\DDK-173.nfo`；
  - AI 分析 3 个 sub-Tab ✓，相似推荐 9 列，状态条 "AI 引擎：默认本地启发式" ✓；
  - 标签聚类 → 2 主题簇（对应 60 高频标签）；
  - 相似推荐 `DDK-173` → 15 部，首行 path 已写入 `UserRole`；
  - 画像解读 → 312 字，本地启发式后端。
- 既有 `tools/smoke_gui.py` 全部通过（重复检测 375 组 / 画像统计 49,436 部 / 明细检索 49 条），
  确认没有破坏老流程。

### 版本

- 对外 `v1.1.3`，内部 `2609070001`（新一天第 1 次构建，UI 主功能调整）。

---

## v1.1.2（内部版本 2609060003 · 2026-09-06）—— 修复扫描无法即时终止

### 修复

- **「终止扫描」无效**（用户反馈）：点击后仍等到全部完成才停止。
  - **根因**：`scanner.py` 原本用 `multiprocessing.Event` 给子进程发停止信号，
    但 PyInstaller `--onefile` 模式下每个子进程都会重新解压到各自的临时目录，
    Event 的 Semaphore 代理跨解压目录同步不可靠，主进程 `set()` 子进程根本看不到。
  - **修复**：改用**哨兵文件**——`tempfile.gettempdir()/nfo_profiler_scan.stop`，
    主线程写文件、worker 子进程每个文件解析前 `os.path.exists()` 检查。
    不依赖任何 multiprocessing 同步层，跨解压目录绝对可靠。
- **修复：终止时丢失已解析记录**：原代码 `as_completed` 循环头部用 `_stop_requested()`
  先 break，导致哨兵触发那一刻刚好完成的批次结果被丢弃。改为**先 `fut.result()` + 落库，
  再判断终止**。
- **修复：哨兵残留**：scan_paths 启动时清理残留哨兵，结束时再清理一次。

### 验证

- 新增 `tools/smoke_stop.py`：1500 个 NFO / 4 进程 / parse_batch=40，
  哨兵在处理到第 114 个文件时触发 → **0.46 s 内停止**、已处理的 **240 条记录全部落库**。
- 旧行为（继续跑完 1500 个文件）→ 现在立即停下并保留已做的工作。

### 版本

- 对外 `v1.1.2`，内部 `2609060003`（同一天第 3 次构建，修 Bug修订号 +1）。

---

## v1.1.1（内部版本 2609060002 · 2026-09-06）—— 界面/扫描/筛选优化

### 界面优化

- **数据源与扫描**界面重构：改为**左右分栏（QSplitter）**，左侧放目录/选项/进度/日志，
  右侧放已登记数据源，空间利用率显著提升；各分组内边距收紧，按钮排布更紧凑。
- 增加**数据库概况**快速统计（作品数 / NFO 数）。
- 扫描进度条改为 `%v / %m (%p%)` 格式，收集阶段显示忙碌动画，解析阶段平滑推进。

### 扫描控制增强

- **新增暂停 / 继续 / 终止扫描按钮**：
  - 暂停：当前批次解析完后主循环阻塞，恢复后继续；
  - 终止：通过 ``multiprocessing.Event`` 通知子进程尽快退出，主循环停止提交新批次，
    已处理的结果保留。
- ``ScanWorker`` 新增 ``pause/resume/stop/toggle_pause`` 方法；
  ``scanner.py`` 的 ``scan_paths`` 新增 ``cancel_event`` / ``pause_event`` / ``parse_batch`` 参数。

### 扫描速度优化

- **批量解析**：子进程由原来的一次处理 1 个文件改为一次处理 ``parse_batch`` 个文件
  （默认 40，界面可调 1–200），显著降低多进程 IPC / pickle 开销。
- 进度上报节流从 300ms 缩短到 **100ms**，并降低按文件数触发阈值，进度条更跟手。
- CLI 实测：500 个合成 NFO / 4 进程 / 无视频探测 → **3.69 s**（133.2 文件/秒）。

### 作品明细增加筛选

- 新增筛选面板：数据源、片商、导演、分辨率、年份范围、评分范围、标签/演员关键词。
- 片商/导演下拉支持从库中自动拉取前 300 个不重复值，且可手动输入；
  扫描完成后自动刷新筛选词表。

### 图标更新

- 按用户提供的 `logo.png` 重新生成 `logo.ico`（16/24/32/48/64/128/256 px 多尺寸），
  窗口标题栏与 exe 图标同步更新。

### 版本

- 对外版本升至 **v1.1.1**；内部版本 **2609060002**。

### 验证

- 编译通过，离屏冒烟（窗口构建 / 重复检测 / 导出 / 画像 / 检索）全部通过；
- 明细筛选实测：片商 `S1 NO.1 STYLE` + 年份 2020–2025 → 36,216 条，结果正确；
- 500 个合成 NFO 扫描成功，进度条实时更新；暂停/终止按钮在 GUI 中可用（未做压力中断测试）。

---

## v1.1.0（内部版本 2609060001 · 2026-09-06）—— 重复影片检测 + 原生桌面 exe

> **Copyright © 2026 肆月Aperture 本软件不得用于商业用途，仅做学习交流使用。**

### 新增：重复影片检测（核心需求）

- **需求原文**：「根据 NFO 信息判断是否有重复文件；若 NFO 位于同一文件夹则可能是视频有几段，
  这种情况排除；主要检测不同文件夹里体现出同一部影片多存一份的情况。」
- **实现**：新增 `nfo_profiler/dedupe.py`（`DuplicateFinder`）。
  1. **身份键**：优先番号 `num`（缺失时回退视频文件名 / 标题开头），归一化为 `abc123`；
     无番号时用「标题（去掉开头番号）+ 年份」。
  2. **同目录 = 分片，排除**：同文件夹内的多份同身份 NFO 判为 CD1/CD2、part1/part2，
     归入 `multipart` 另册统计，**不计入重复**。
  3. **跨目录 = 重复**：按文件夹归并成「份」，份数 ≥ 2 才判定为重复组；
     保留最大一份，其余份体积之和 = **可回收空间**。
  4. **置信度**：番号一致=高，标题+年份=中，仅标题=低；各份体积完全一致上调为「极高」；
     分辨率/体积差异较大时标注「可能是不同版本」。
  5. **导出**：CSV（UTF-8 BOM）/ XLSX（概览 + 重复组 + 成员明细 + 已排除分片）/ JSON，
     均带版权声明行。
- **踩坑修复**：部分 NFO 的 `num` 只存了**前缀**（如 `T38`，真实番号 `T38-002`），
  直接用它当身份键会把同一前缀下 21 部不同作品误并成一组。
  `extract_num()` 增加「细化」逻辑：番号键数字部分 ≤ 2 位且文件名/标题能给出更长的同源键时，
  采用更长的那个。修复前误报 21 份 / 48 GB，修复后该组消失。
- **界面**：桌面端新增「④ 重复检测」标签页（树形分组、双击打开所在目录、复制路径、一键导出）。
- **命令行**：新增 `nfo_profiler.exe dedupe`（`--source` / `--num-only` / `--title-only` /
  `--min-confidence` / `--top` / `--out` / `--format`）。
- **实测**（5.3 万部作品 / 8 个数据源）：耗时 **2.3 s**，检出 **368 组**重复（冗余 369 份、
  可回收约 **116.85 GB**），同目录分片 **646 组 / 1729 个 NFO** 自动排除。

### 重构：界面改为原生桌面窗口（不再依赖浏览器）

- 新增 `nfo_profiler/gui.py`（**PySide6 / Qt 6**），双击 exe 直接弹窗：
  ① 数据源与扫描 ② 画像概览 ③ 作品明细 ④ 重复检测 ⑤ 导出与报告 ⑥ AI 分析 ⑦ 关于与声明。
- 所有耗时操作（扫描 / 统计 / 检索 / 检测 / 导出 / AI）走 `QThread` + Signal，
  主线程只刷界面；**严禁**在子线程里操作控件（首版踩过，已改为信号回传）。
- 深色 Fusion 主题；状态栏与「关于」页常驻版权声明。
- `run.py`：无参数 → 桌面窗口；`--web` / `ui --web` 才走旧版浏览器界面（`webui.py` 保留兼容）。
- `cli.py`：`ui` 子命令默认启动桌面窗口，新增 `--web` 开关；新增 `dedupe` 子命令。

### 版权与声明

- 统一常量 `nfo_profiler.COPYRIGHT_NOTICE` =
  **「Copyright © 2026 肆月Aperture 本软件不得用于商业用途，仅做学习交流使用。」**
  落到：`gui.py` 关于页 / 状态栏、`dedupe.py` 模块头与全部导出文件（CSV/XLSX/JSON）、
  `run.py` 头、`report.py` 报告页脚、`webui.py` 页脚、`README.md`、`LICENSE`。

### 版本与发布约定

- `__version__ = "1.1.0"`、`__build__ = "2609060001"`，新增 `version_text()`；
  界面标题显示 `v1.1.0 (Build 2609060001)`。
- 最新 exe 放**项目根目录** `nfo_profiler.exe`；旧版移入 **`history/`**
  （`nfo_profiler_v1.0.9_20260831.exe`、`nfo_profiler_v109_20260831.exe`），
  并另存一份本次构建 `history/nfo_profiler_v1.1.0_2609060001.exe`。
- 更新 `杀死进程.bat`：除按端口 9527 结束外，增加按进程名 `nfo_profiler.exe` 结束。
- 新增 `tools/smoke_gui.py`（离屏冒烟测试：窗口构建 → 重复检测 → 导出 → 画像 → 检索）。

### 交付产物

| 文件 | 说明 |
| --- | --- |
| `nfo_profiler.exe` | v1.1.0 独立桌面 exe（85.7 MB，PySide6 + PyInstaller onefile，无控制台黑框） |
| `history/*.exe` | 历史版本留档 3 份 |
| `nfo_profiler/dedupe.py` | 重复检测模块（新增） |
| `nfo_profiler/gui.py` | 原生桌面界面（新增） |

### 验证

- 离屏冒烟（`QT_QPA_PLATFORM=offscreen`）：窗口构建 OK、重复检测 368 组、
  CSV/JSON 导出成功、画像统计 53,374 部 / 127.04 TB、明细检索 49 条 —— **全部通过**。
- 打包后实跑：`nfo_profiler.exe scan ... --workers 4`（验证冻结模式多进程正常）、
  `nfo_profiler.exe dedupe --out ... --format csv,json,xlsx`（三种格式全部生成）。
- 构造用例验证规则：`Disk1\AAA-111` 与 `Disk2\AAA-111` → 判为 **1 组重复**；
  `Disk1\BBB-222` 下的 `cd1/cd2` 两份 → 归入 **同目录分片并排除**。
- 双击启动验证：进程窗口标题 `NFO 画像矿工 · v1.1.0 (Build 2609060001)`，响应正常。

---

## v1.0.9（2026-08-31）—— 扫描进度实时刷新

### 修复 / 优化
- **扫描进度实时显示**（用户反馈 #1）：此前扫描仅在每 500 个文件（或末尾）才上报一次进度，
  导致进度条「卡住」、文件名不更新，直到全部解析完才一次性跳到 `[done]`。
  - `scanner.py`：`scan_paths` 的进度上报从「仅批处理满 `chunk` 时上报」改为**双节流**策略——
    既按文件数（`emit_every = max(50, chunk//10)`，约每 50 个文件），也按墙钟时间（最快每 300ms）
    调用 `_maybe_emit()`，保证后端 `done` 计数与 `current` 文件名平滑推进。
  - `webui.py`：前端 `pollScan()` 轮询间隔由 **600ms 降至 300ms**，配合后端 300ms 节流，
    进度条与「正在解析：<路径>」实时刷新；`.bar i` 保留 0.35s 过渡动画，视觉顺滑。
- 单文件解析时（`n_workers == 1`）同样启用该节流逻辑，行为一致。

### 验证
- 单进程路径 1200 个 NFO 实测：进度回调触发 **25 次**（旧逻辑仅 ~2–3 次），
  步长稳定在每 50 个文件一次；大库（数万文件、耗时分钟级）下时间节流亦会介入，UI 亚秒级更新。

---

## v1.0.8（2026-08-31 晚）—— 打包声明版（当前全量源码）

### 本次打包说明
- 基于**当前全量源码**（含 v1.0.7 聚类/相似推荐、v1.0.8 画像解读、v1.0.9/1.0.10 语义向量检索等全部 AI 特性）重新打包，版本号定为 **v1.0.8**。
- 新增**版权与使用限制声明**（Copyright 2026 肆月Aperture；本软件不得用于商业用途，仅做学习交流使用），落到四处：
  - `README.md` 新增「十六、版权与使用限制」章节；
  - `LICENSE` 版权持有者更正为「肆月Aperture」；
  - 运行界面（webui 侧边栏页脚）常驻显示声明；
  - **生成的画像报告页脚**注明版权 + 非商用声明。

## v1.0.7（2026-08-31）

**新增：AI 分析标签页 —— 标签主题聚类 + 相似作品推荐（离线优先，可升级）**

### 新增 `AIEngine` 离线能力（`ai_engine.py`）
- `cluster_tags(store, top_n, min_co)`：基于**标签共现**的贪心主题聚类。
  取高频标签为种子，把在 >= ``min_co`` 部作品里共同出现过的标签归入同一主题，
  产出「题材 / 主题」分组（如 爱情 / 剧情 / 特定类型聚合）。
- `similar_works(store, num, limit)`：基于**标签 + 演员 Jaccard 相似度**的相似作品推荐
  （共享越多、并集越小越相似）。
- 两者均**零依赖、离线可用**；未来 MiniLM 可用时改为句向量聚类 / 余弦相似度，
  Ollama 可用时由模型归纳主题名——保持同一接口、自动升级。

### Web UI 改动（`webui.py`）
- 新增第三个标签页 **AI 分析**：未开启 AI 增强时显示提示；开启后提供
  「🏷️ 标签主题聚类」按钮与「🔍 相似作品推荐」查询框。
- 新增端点 `/api/ai/clusters`、`/api/ai/similar?num=`，AI 未开启时返回 `disabled` 标记。
- `AppState` 增加聚类结果缓存，扫描完成后失效重建。

### 本地化 AI 能力落地进度
- ✅ 开关 + 启发式语义检索（v1.0.6）
- ✅ 标签聚类 + 相似推荐（v1.0.7，离线可用）
- ⬜ MiniLM 句向量 / Ollama 语义（接口已预留，待用户安装本地模型后自动启用）

---

---
## v1.0.6（2026-08-31）

**新增：本地 AI 增强开关（可选 · 惰性 · 不阻塞主线程）**

### 设计红线（吸取 v1.0.4 教训）
- 模块加载只 import 标准库，**绝不** import torch / onnxruntime / tokenizers；
- 重依赖（onnxruntime、tokenizers）仅在「后台线程」惰性 import，并带异常隔离；
- 检测不到任何本地 AI 环境时，自动降级为「本地启发式增强」（同义词 / 异体字扩展），
  **保证开关永远可用、HTTP 服务永不被拖垮**。

### 新增 `nfo_profiler/ai_engine.py`
- `AIEngine`：状态机 `idle → initializing → ready`，后端分三层
  - `ollama`：检测到本地 Ollama（`http://localhost:11434`）时，用 `/api/generate` 把自然语言查询改写成检索关键词；
  - `minilm`：检测到本地 MiniLM ONNX 模型（`ai_models/model.onnx` + `tokenizer.json`）时，惰性加载 onnxruntime 做句向量（预留接口）；
  - `heuristic`：兜底，零依赖，拆词 + 同义词 / 异体字扩展后做 OR 检索。

### Web UI 改动（`webui.py`）
- **运行状态**分组下新增 🤖 **AI 增强** 开关（滑动 switch + 实时状态 chip）；
- 新增端点 `/api/ai/state`、`/api/ai/enable`、`/api/ai/disable`，开关状态轮询（1.5s）；
- 明细检索接入 AI：开启后查询走 `AIEngine.expand_terms()`，结果行展示 `🤖 AI 增强（backend）` 标注；
- 网络调用（Ollama）刻意放在 `AppState` 锁外，避免阻塞扫描线程。

### 本地化 AI 能力调研结论
- 纯本地可做的增强：语义检索 / 自然语言查询改写 / 标签聚类 / 相似作品推荐 / 画像摘要生成；
- 轻量推理首选 **onnxruntime + MiniLM**（~25–80MB，CPU 友好，无需 torch）；大模型走 **Ollama** 本地 HTTP。

---

---
## v1.0.5（2026-08-31）

**修复 + 美化：JS 语法修复 / 自定义图标 / 标题更新 / 目录清理**

### 修复：目录浏览器卡在"加载中…"——JS 语法错误导致整个 `<script>` 块不执行

- **根因**：`webui.py` 的 HTML 模板中，Python 源码 `'\''` 经转义后输出为 JS 的 `\'`，被 JS 引擎解析为「字符串结束 + 意外字符 `:`」，触发 **SyntaxError**。由于错误位于 `<script>` 块中，**整个 JavaScript 完全不执行**，页面永远停在 HTML 默认的"加载中…"静态文本。
- **影响范围**：3 处同类转义问题（`renderCrumb()` 路径拼接 ×2 + `btnUp` 的 `lastIndexOf`）。
- **修复方式**：JS 字符串中的反斜杠统一改用双引号包裹，Python 源码中使用 **4 个反斜杠** (`\\\\`) 输出 JS 所需的 `\\`。

### 新增：自定义图标

- 将 `logo.png`（用户提供的放大镜+用户图标）转换为 `logo.ico`（多尺寸 16~256px）。
- **exe 图标**：PyInstaller 构建加 `--icon logo.ico`，任务栏/文件管理器显示新图标。
- **网页 favicon**：新增 `/api/logo` 端点提供 PNG 图标；`<head>` 加 `<link rel="icon">`；页面 header 左侧显示 22px 圆角 logo。
- Logo 数据文件通过 `--add-data "logo.png;logo.png"` 打包进 exe，运行时从 `sys._MEIPASS` 读取。

### 文案更新

- 全局标题「用户画像报告」→「用户×癖分析报告」（涉及 `report.py`、`webui.py` 空报告页、`exporter.py` Markdown 导出）。

### 清理

- 删除构建缓存（`__pycache__/`、`build/`、`*.pyc`、`*.spec`）、旧版备份（`nfo_profiler_v104.exe`）、源文件（`logo.psd`）、测试数据（`_out/` 143MB）、运行日志。项目目录从 ~198MB 精简至 **16MB**。

---

---
## v1.0.4（2026-08-31）

**重构：彻底移除 AI/GPU 加速与运行环境检测，回归纯本地核心模式**

- **背景**：v1.0.3 的环境检测虽加了超时保护，但用户机器上 torch 处于损坏/半安装状态，`import torch` 一旦调用会挂起并**长期占用 GIL（全局解释器锁）**，连带把整个 HTTP 服务线程拖死——表现为"运行环境"和"＋ 添加当前目录"**同时卡死**（后者本质是同一个 GIL 阻塞的表症，并非独立 Bug）。超时保护只能"放弃等结果"，那个挂起的线程仍占着 GIL，服务依旧不可用。
- **决策**：按用户要求，直接拆除 AI/GPU 加速与运行环境检测，回归最初设计——仅用 Python 标准库完成 NFO 解析 → 画像统计 → 报告 → 导出。
- **删除**：
  - `nfo_profiler/envcheck.py`（运行环境检测的全部实现）
  - `nfo_profiler/ai.py`（AI 语义归并的全部实现）
  - `cli.py` 的 `doctor`、`ai-merge` 两个命令
- **`webui.py` 改动**：
  - 移除 `from .envcheck import check_environment` 导入
  - 移除 `AppState.deps` 字段及 `/api/deps` 接口、后台检测线程 `_run_deps`
  - 前端移除 `renderDeps`/`fetchDeps` 及对应轮询
  - 左侧"运行环境"区块替换为静态**"运行状态：核心模式·纯本地运行"**说明，明确"无需 GPU/AI/第三方依赖，开箱即用"
- **效果**：启动不再 `import torch`，服务全程零 GIL 阻塞；目录浏览、扫描、报告、导出全部即时响应。

---

---
## v1.0.3（2026-08-31）

**修复：运行环境检测卡在"检测中…"无进度、无反馈**

- **根因**：`import torch` 无超时保护——torch 损坏/缺失/部分安装时可能无限挂起，导致后台检测线程永远不返回，前端永远显示"检测中…"。
- **`_torch_info()` 加 10 秒超时保护**：用 `concurrent.futures.ThreadPoolExecutor` + `result(timeout=10)` 包裹 `import torch`，超时自动降级为"未安装"，不再卡死整个检测流程。
- **新增 `on_checking` 前置回调**：每项检测**开始前**即通知前端（而非完成后），用户可实时看到「正在检测：PyTorch（6/9）」这样的进度。
- **前端进度展示增强**：
  - 加 spinner 旋转动画 + 进度条呼吸动画，视觉上明确表示"正在检测中";
  - 检测完成后显示汇总（N 项通过 / N 项警告 / N 项异常）+ 推荐文字;
  - 检测失败/异常时给出明确提示，并强调"不影响核心功能"。
- **设计原则强化**：环境检测结果**仅决定是否启用 AI/GPU 加速**，核心扫描与报告功能在任何检测结果下均可正常使用。即使全部检测异常，软件仍可完成 NFO 解析 → 统计 → 报告的完整流程。

**其他**
- `nvidia-smi` 超时由 8s 缩短到 6s。
- 检测项固定为 9 项（"运行方式"为附加项不计入进度总数），进度条百分比更准确。

---

---
## v1.0.2（2026-08-31）

**修复：启动即报 `sqlite3.OperationalError: database is locked`**

- 根因：旧逻辑在 9527 端口被占用时会**顺延到 9528/9529 再起一个服务**，但两个实例仍打开**同一个** `output/nfo.db`，抢库锁，第二个在初始化写 `meta` 表时崩溃。该报错与「运行环境检测」无关（环境检测在 `AppState` 创建之后的后台线程才启动）。
- 新增**单实例守护**：启动时若 9527 已被「自己的」实例占用（探测 `/api/status` 含 `schema_version` 字段即判定为本程序），直接把浏览器指向现有实例并退出，不再起第二个服务、不再抢库。
- 端口回退循环同步加防御：主端口被自己占用时同样转向现有实例，不另开服务。

**修复：SQLite 并发抢锁隐患**

- `ThreadingHTTPServer` 多线程下，扫描线程写库、请求线程读库会并发访问同一连接。给 `Store` 所有 DB 访问加 `threading.RLock` 串行化，杜绝运行期偶发 `database is locked` 与潜在数据错乱。
- 连接加固：新增 `PRAGMA busy_timeout=30000`；初始化建表/写 `meta` 失败重试最多 5 次；若仍被锁，抛出**中文可读报错**（而非原生 traceback），提示先关闭其它实例或用 `kill.bat`。

**其他**

- 版本号由 `1.0.1` 升为 `1.0.2`。

---

---
## v1.0.1（2026-08-31）

**运行环境检测（Bug1）**
- 检测改为**后台线程**执行，双击启动后网页**立即打开**，不再被检测阻塞。
- 新增**检测项清单 + 进度提示**：网页顶部实时显示「正在检测：xxx（n/总数）」、进度条，以及逐项检测结果的标签（ok/warn）。
- `nvidia-smi` 探测超时由 20s 缩短到 8s，整体更轻快。

**画像报告悬浮说明（Bug2）**
- 在报告三处关键指标增加**鼠标悬停说明（tooltip）**：
  - 「高频标签 · 标签频次 Top N」——解释按作品数排序、占比含义、元数据噪音已剥离；
  - 「高频艺人 · 作品数 Top N」——解释按作品数排序、异写归并规则；
  - 「作品明细」——解释报告内嵌条数及完整数据导出方式。
- 「标签频次 Top N」「作品数 Top N」标题改为**动态数字**，与左侧设置的 TopN 一致。

**报告空态 / 运行中占位美化（Bug3）**
- 未生成报告时，网页不再是一片纯白，改为自包含美化页：渐变标题、图标、状态说明。
- 占位页**轮询后端**，未扫描时给出操作引导；扫描进行中显示**实时进度条与当前文件**；扫描完成后一键「生成报告」。
- 主界面报告 iframe 初始即加载该占位页。

**其他**
- 版本号由 `1.0.0` 升为 `1.0.1`。

---

---
## v1.0.0（历史基线）

- 初版发布：NFO 解析、SQLite 仓储、多进程扫描、用户画像/高频统计、HTML 报告、多格式导出、本地 Web 界面。
- 多路径扫描、本地 AI（GPU）语义聚类、运行环境检测。
- 打包为根目录双击即开的 windowed exe（端口 9527），含 `kill.bat`。

