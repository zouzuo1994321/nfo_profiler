# NFO 画像矿工 —— 依赖声明
# 核心功能（扫描 / 画像 / 导出 CSV·JSON·MD·HTML / Web UI）为 Python 标准库实现，零第三方依赖。
# 以下均为【可选】依赖，缺失时对应功能自动降级并提示。

# ── 基础可选（推荐）─────────────────────────────────────
# XLSX 导出需要（缺省时 CSV/HTML 等其它格式不受影响）
openpyxl>=3.1

# ── 开发 / 测试 ─────────────────────────────────────────
# 生成合成测试数据用于压测（tools/make_sample_nfo.py）
# 无需安装，运行即可
