# NFO 画像矿工 —— 依赖声明
# 核心功能（扫描 / 画像 / 导出 CSV·JSON·MD·HTML / 重复检测）为 Python 标准库实现，零第三方依赖。
# 以下均为【可选 / 分场景】依赖，缺失时对应功能自动降级并提示。

# ── 桌面界面（v1.1.0 起的主界面）─────────────────────────
# 双击 exe 的原生窗口需要；只用命令行（scan/report/export/dedupe）则不必安装
PySide6>=6.5

# ── 基础可选（推荐）─────────────────────────────────────
# XLSX 导出需要（缺省时 CSV/HTML 等其它格式不受影响）
openpyxl>=3.1

# ── 开发 / 打包 ─────────────────────────────────────────
# 打包独立 exe（详见 README「从源码打包 exe」）
# pyinstaller>=6.0

# ── 本地 AI 增强（可选中的可选）──────────────────────────
# 句向量语义检索：onnxruntime + tokenizers（模型放 ai_models/）
# 大模型解读 / 查询改写：本机安装 Ollama（http://localhost:11434）
# 两者都没有时自动降级为「离线启发式」，功能不受影响
